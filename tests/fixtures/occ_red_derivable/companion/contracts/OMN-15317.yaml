---
schema_version: "1.0.0"
ticket_id: "OMN-15317"
title: "Autobind OCC evidence for OMN-15317"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1922."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1922 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1922 on OmniNode-ai/omnimarket changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (none reported) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1922"
    description: "PR #1922 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/occ_content_probe.py?ref=343202f283c3734b22f16d5d2f5af69083de5bb7 --jq '.content' | base64 -d | grep -c 'class SymbolCandidate'
  - id: "dod-OmniNode-ai-omnimarket-pr-1922-ci"
    description: "PR #1922 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1922 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
