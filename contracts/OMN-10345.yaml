---
schema_version: "1.0.0"
ticket_id: OMN-10345
title: "Target market runtime requests by runtime address"
summary: "Add optional runtime:// target addressing to the Codex Pattern B request path and keep Codex skill shims aligned with explicit runtime targeting."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Focused Pattern B client tests prove addressed runtime targeting for all current Codex market commands without live runtime startup."
    command: "env -u PYTHONPATH uv run pytest tests/test_codex_runtime_client.py -q"
  - kind: "manual"
    description: "Runtime client and tests pass lint."
    command: "env -u PYTHONPATH uv run ruff check src/omnimarket/adapters/codex/runtime_client.py tests/test_codex_runtime_client.py"
  - kind: "manual"
    description: "Codex plugin shims document the optional target runtime address flag while remaining dispatch-only."
    command: "env -u PYTHONPATH uv run pytest tests/test_codex_plugin_shims.py -q"
  - kind: "manual"
    description: "Broker-free compile-only preflight proves market skill requests compile addressed command envelopes without Kafka or live runtime startup."
    command: "env -u PYTHONPATH uv run pytest tests/test_codex_runtime_client.py::test_market_plugin_commands_compile_without_broker tests/test_codex_runtime_client.py::test_main_compile_only_outputs_command_without_broker -q"
  - kind: "manual"
    description: "No live runtime or .201 mutation is performed by this proof."
    command: "PR body states no containers were started, no deploy/restart/install was performed, and .201 was not mutated."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Pattern B in-memory broker tests prove runtime address propagation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_codex_runtime_client.py -q"
  - id: "dod-002"
    description: "CLI accepts --target-runtime-address and serializes it onto the broker command."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "rg -n \"target-runtime-address|target_runtime_address|ONEX_TARGET_RUNTIME_ADDRESS\" src/omnimarket/adapters/codex/runtime_client.py tests/test_codex_runtime_client.py"
  - id: "dod-003"
    description: "Codex skill shims document target runtime address selection without adding execution logic."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_codex_plugin_shims.py -q"
  - id: "dod-004"
    description: "Compile-only preflight validates addressed broker command envelopes without publishing to Kafka."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_codex_runtime_client.py::test_market_plugin_commands_compile_without_broker tests/test_codex_runtime_client.py::test_main_compile_only_outputs_command_without_broker -q"
  - id: "dod-005"
    description: "Post-merge deploy/readiness smoke plan: runtime client imports and validates a runtime:// target address in the deployed runtime container."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python - <<'PY'\nfrom omnimarket.adapters.codex.runtime_client import ModelPatternBBrokerClientRequest\nrequest = ModelPatternBBrokerClientRequest(command_name='aislop_sweep', target_runtime_address='runtime://omninode-pc/stability-test/main')\nprint(request.target_runtime_address)\nPY"
