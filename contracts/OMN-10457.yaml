---
schema_version: "1.0.0"
ticket_id: OMN-10457
title: "Add AB compare registry endpoint configurability"
summary: "Adds env-backed GLM registry endpoint and model-id resolution for the AB compare orchestrator, including blank-env fallback behavior so laptop and .201 demo profiles can use different cloud baselines without hardcoding endpoints."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Focused AB compare orchestrator registry tests pass."
    command: "uv run pytest tests/unit/nodes/node_ab_compare_orchestrator/test_contract_schema.py tests/unit/nodes/node_ab_compare_orchestrator/test_handler_ab_compare_orchestrator.py -q"
  - kind: "manual"
    description: "Ruff passes for the AB compare orchestrator registry changes."
    command: "uv run ruff check src/omnimarket/nodes/node_ab_compare_orchestrator tests/unit/nodes/node_ab_compare_orchestrator"
  - kind: "manual"
    description: "Repo-local OMN-10457 deploy contract validates."
    command: "uv run validate-yaml contracts/OMN-10457.yaml"
  - kind: "manual"
    description: "Post-merge deploy smoke should verify that AB compare registry resolution still imports; no live deploy or runtime restart was performed in this PR."
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_ab_compare_orchestrator.handlers.handler_ab_compare_orchestrator import HandlerABCompareOrchestrator; print('ab compare registry deploy smoke ok')\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused AB compare orchestrator registry tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_ab_compare_orchestrator/test_contract_schema.py tests/unit/nodes/node_ab_compare_orchestrator/test_handler_ab_compare_orchestrator.py -q"
  - id: "dod-002"
    description: "Ruff passes for the AB compare orchestrator registry changes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_ab_compare_orchestrator tests/unit/nodes/node_ab_compare_orchestrator"
  - id: "dod-003"
    description: "Repo-local OMN-10457 deploy contract validates."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10457.yaml"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke should verify that AB compare registry resolution still imports; no live deploy or runtime restart was performed in this PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_ab_compare_orchestrator.handlers.handler_ab_compare_orchestrator import HandlerABCompareOrchestrator; print('ab compare registry deploy smoke ok')\""
