---
schema_version: "1.0.0"
ticket_id: OMN-10338
title: "Add GPU-time evidence to compute-cost projection"
summary: "Extend the LLM cost projection with GPU usage evidence and compute-cost manifest pricing."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #451 checks pass, including Golden Chain Suite, E2E Workflow Runner, contract-validation, and deploy-gate"
    command: "gh pr checks 451 --repo OmniNode-ai/omnimarket --watch"
  - kind: "tests"
    description: "GPU compute-cost projection golden-chain replay passes"
    command: "uv run pytest tests/test_golden_chain_projection_llm_cost.py -q"
  - kind: "manual"
    description: "Post-merge deploy smoke confirms the changed projection handler imports in the deployed runtime container"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_projection_llm_cost.handlers.handler_projection_llm_cost import HandlerProjectionLlmCost; print('deploy smoke ok', HandlerProjectionLlmCost.__name__)\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "GPU compute-cost projection golden-chain replay passes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_projection_llm_cost.py -q"
  - id: "dod-002"
    description: "Touched projection handler passes lint and deploy-readiness import smoke"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_projection_llm_cost/handlers/handler_projection_llm_cost.py tests/test_golden_chain_projection_llm_cost.py && uv run python -c \"from omnimarket.nodes.node_projection_llm_cost.handlers.handler_projection_llm_cost import HandlerProjectionLlmCost; print('deploy readiness smoke', HandlerProjectionLlmCost.__name__)\""
  - id: "dod-003"
    description: "Post-merge deploy smoke plan for the changed projection handler"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_projection_llm_cost.handlers.handler_projection_llm_cost import HandlerProjectionLlmCost; print('deploy smoke ok', HandlerProjectionLlmCost.__name__)\""
