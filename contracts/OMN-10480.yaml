---
schema_version: "1.0.0"
ticket_id: OMN-10480
title: "Add ab-compare reducer node"
summary: "Add a pure ab-compare reducer node that accumulates inference results, computes per-model costs, and materializes sorted comparison output."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
evidence_requirements:
  - kind: "manual"
    description: "Focused ab-compare reducer unit tests pass"
    command: "uv run pytest tests/nodes/node_ab_compare_reducer/ -q"
  - kind: "manual"
    description: "Node metadata dependency scope validates reducer imports"
    command: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - kind: "manual"
    description: "Node drift gate validates changed reducer package registration"
    command: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - kind: "ci"
    description: "omnimarket PR #510 checks pass, including lint, deploy gate, and receipt gate"
    command: "gh pr checks 510 --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused ab-compare reducer unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_ab_compare_reducer/ -q"
  - id: "dod-002"
    description: "Node metadata dependency scope validates reducer imports"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - id: "dod-003"
    description: "Node drift gate validates changed reducer package registration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the ab-compare reducer handler and models in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_ab_compare_reducer.handlers.handler_ab_compare_reducer import HandlerAbCompareReducer; from omnimarket.nodes.node_ab_compare_reducer.models.model_ab_compare_state import ModelAbCompareState; print('ab-compare reducer deploy smoke ok')\""
