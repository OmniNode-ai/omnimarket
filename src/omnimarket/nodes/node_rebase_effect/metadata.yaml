name: node_rebase_effect
version: "1.0.0"
description: "Rebases PR branches in isolated worktrees for merge-sweep remediation"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_rebase_effect: "omnimarket.nodes.node_rebase_effect"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: true
  requires_docker: false
  side_effect_class: external_io
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["merge-sweep", "git", "rebase", "effect"]
pack: "merge-sweep"
display_name: "Rebase Effect"
node_role: "effect"
