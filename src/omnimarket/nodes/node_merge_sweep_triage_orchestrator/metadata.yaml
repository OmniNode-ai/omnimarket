name: node_merge_sweep_triage_orchestrator
version: "1.0.0"
description: "Fans out merge-sweep triage decisions into remediation commands"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_merge_sweep_triage_orchestrator: "omnimarket.nodes.node_merge_sweep_triage_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["merge-sweep", "triage", "orchestrator"]
pack: "merge-sweep"
display_name: "Merge Sweep Triage Orchestrator"
node_role: "orchestrator"
