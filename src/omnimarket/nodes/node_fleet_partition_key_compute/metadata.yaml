name: node_fleet_partition_key_compute
version: "1.0.0"
description: "Fleet topology keying COMPUTE (D-topology): deterministic, injective repo:branch partition key so Kafka's same-key-same-partition guarantee keeps one branch's stream on a single partition (OMN-14978)."
pack: distributed_validation
node_role: compute
display_name: "Fleet Partition Key Compute"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_fleet_partition_key_compute: "omnimarket.nodes.node_fleet_partition_key_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: none
dependencies: []
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "fleet-topology", "partition-keying", "distributed-validation", "OMN-14978"]
