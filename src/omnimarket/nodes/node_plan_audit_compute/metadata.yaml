name: node_plan_audit_compute
version: "1.0.0"
description: "YAML validation, field checks, and cycle detection for plan files (STUB)"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_plan_audit_compute: "omnimarket.nodes.node_plan_audit_compute"
capabilities:
  standalone: false
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "plan", "audit", "validation", "yaml"]
pack: "planning"
display_name: "Plan Audit Compute"
node_role: "compute"
