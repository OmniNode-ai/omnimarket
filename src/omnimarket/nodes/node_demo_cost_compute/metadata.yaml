name: node_demo_cost_compute
version: "1.0.0"
description: "Pricing lookup and cost calculation from per-model inference results"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_demo_cost_compute: "omnimarket.nodes.node_demo_cost_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: pure
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["demo", "cost", "compute", "pricing"]
pack: "demo"
display_name: "Demo Cost Compute"
node_role: "compute"
