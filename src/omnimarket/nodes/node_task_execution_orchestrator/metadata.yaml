name: node_task_execution_orchestrator
version: "1.0.0"
description: "Generic task.execute route planner — normalizes prompt/contract, maps to existing routes, and composes the verification and delegation route authorities (opt-in)"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_task_execution_orchestrator: "omnimarket.nodes.node_task_execution_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["task", "execute", "route", "orchestration", "planner"]
pack: "task-execution"
display_name: "Task Execution Orchestrator"
node_role: "compute"
