name: node_changelog_audit_compute
version: "1.0.0"
description: "Parse and classify caller-supplied changelog content for repositories"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_changelog_audit_compute: "omnimarket.nodes.node_changelog_audit_compute"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "changelog", "audit", "dependencies"]
pack: "audit"
display_name: "Changelog Audit Compute"
node_role: "compute"
