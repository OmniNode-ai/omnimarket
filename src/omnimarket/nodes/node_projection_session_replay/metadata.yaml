name: node_projection_session_replay
version: "1.0.0"
description: "Session replay projection reducer — reduces session lifecycle events into queryable replay snapshots for the omnidash Session Replay widget"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_projection_session_replay: "omnimarket.nodes.node_projection_session_replay"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: write_db
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["reducer", "projection", "session", "replay", "dashboard"]
pack: "data"
display_name: "Projection: Session Replay"
node_role: "reducer"
