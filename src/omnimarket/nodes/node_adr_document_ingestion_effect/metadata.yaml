name: node_adr_document_ingestion_effect
version: "0.1.0"
description: "ADR document ingestion effect — crawls markdown files and extracts git metadata for the ADR canary pipeline"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_adr_document_ingestion_effect: "omnimarket.nodes.node_adr_document_ingestion_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["adr", "ingestion", "effect", "filesystem", "read-only"]
pack: "adr"
display_name: "ADR Document Ingestion Effect"
node_role: "effect"
tickets: ["OMN-10693"]
