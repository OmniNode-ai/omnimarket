name: node_stub_detector
version: "1.0.0"
description: "Pure deterministic detector of unimplemented method stubs in an ONEX node source — no LLM calls, no I/O"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_stub_detector: "omnimarket.nodes.node_stub_detector"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
  - "omnibase-compat>=0.3.1"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "ast", "stub-detection", "codegen", "deterministic"]
pack: "core"
display_name: "Stub Detector"
node_role: "compute"
