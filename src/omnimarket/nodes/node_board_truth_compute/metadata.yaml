name: node_board_truth_compute
version: "1.0.0"
description: "Pure computation: derives each ticket's true board state from ledger, PR, branch, and board facts"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_board_truth_compute: "omnimarket.nodes.node_board_truth_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "board-truth", "projection", "ticket-triage"]
pack: "planning"
display_name: "Board Truth"
node_role: "triage"
