name: node_occ_observation_projection
version: "1.0.0"
description: "Pure computation: dedupes the raw append-only OCC observation trail into one deterministic representative observation per distinct exact source tuple. Storage-agnostic, zero I/O."
pack: occ_companion
node_role: compute
display_name: "OCC Observation Projection"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_occ_observation_projection: "omnimarket.nodes.node_occ_observation_projection"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "occ", "autoauthor", "report-only"]
