name: node_adr_decision_extraction_llm_effect
version: "1.0.0"
description: "Decision extraction effect node that identifies architectural decisions, pivots, doctrine, lessons, supersessions, and rejected approaches from segmented document content using a caller-specified LLM."
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_adr_decision_extraction_llm_effect: "omnimarket.nodes.node_adr_decision_extraction_llm_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: external_io
dependencies:
  - "omnibase_core>=0.39.0"
  - "httpx>=0.27.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "adr", "decision-extraction", "llm", "canary"]
pack: "workflow"
display_name: "ADR Decision Extraction LLM Effect"
node_role: "effect"
