name: node_ledger_hash_compute
version: "1.0.0"
description: "Computes deterministic ledger journal hash records"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_ledger_hash_compute: "omnimarket.nodes.node_ledger_hash_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["ledger", "hash", "compute", "proof"]
pack: "ledger"
display_name: "Ledger Hash Compute"
node_role: "compute"
