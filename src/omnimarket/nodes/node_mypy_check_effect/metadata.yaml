name: node_mypy_check_effect
version: "1.0.0"
description: "Runs mypy over a code artifact and returns typed diagnostics — an EFFECT that owns subprocess + filesystem I/O"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_mypy_check_effect: "omnimarket.nodes.node_mypy_check_effect"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "mypy", "type-check", "codegen", "validation"]
pack: "core"
display_name: "Mypy Check Effect"
node_role: "effect"
