name: node_persona_lifecycle_orchestrator
version: "0.1.0"
description: "Coordinates persona snapshot lifecycle rebuilds from tick and on-demand commands"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_persona_lifecycle_orchestrator: "omnimarket.nodes.node_persona_lifecycle_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["persona", "lifecycle", "orchestrator"]
pack: "memory"
display_name: "Persona Lifecycle Orchestrator"
node_role: "orchestrator"
