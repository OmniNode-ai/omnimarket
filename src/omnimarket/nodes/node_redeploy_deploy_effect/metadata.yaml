name: node_redeploy_deploy_effect
version: "1.0.0"
description: "Deploy publish-monitor + rollback effect — Kafka request-reply with the .201 deploy agent."
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_redeploy_deploy_effect: "omnimarket.nodes.node_redeploy_deploy_effect"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: external_io
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "deploy", "runtime", "kafka", "rollback", "publish-monitor", "OMN-13211"]
pack: "release"
display_name: "Redeploy Deploy Publish-Monitor Effect"
node_role: "effect"
