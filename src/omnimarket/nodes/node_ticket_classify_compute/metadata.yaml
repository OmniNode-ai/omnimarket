name: node_ticket_classify_compute
version: "1.0.0"
description: "Ticket classify compute — keyword heuristic buildability classification"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_ticket_classify_compute: "omnimarket.nodes.node_ticket_classify_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: pure
dependencies:
  - "omnibase_core>=0.39.0"
  - "pyyaml>=6.0.2"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "build-loop", "classification", "ticket-triage"]
