name: node_report_anchor_probe_effect
version: "1.0.0"
description: "Live content-anchor re-probes (git SHA / artifact path / PR number) for dispatch reports, feeding the OMN-15163 COMPUTE validator"
omnibase_core_compat: ">=0.46.7,<0.47.0"
entry_points:
  onex.nodes:
    node_report_anchor_probe_effect: "omnimarket.nodes.node_report_anchor_probe_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: "read"
dependencies:
  - "omnibase_core>=0.46.7"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "report", "anchor", "probe", "git", "validation"]
pack: "orchestration"
display_name: "Report Anchor-Probe Effect"
node_role: "effect"
