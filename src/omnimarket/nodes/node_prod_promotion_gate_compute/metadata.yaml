name: node_prod_promotion_gate_compute
version: "1.0.0"
description: "Pure deterministic prod-promotion gate — no I/O, no bus, no model calls."
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_prod_promotion_gate_compute: "omnimarket.nodes.node_prod_promotion_gate_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: none
dependencies:
  - "omnibase_core>=0.39.0"
  - "omnibase-compat>=0.3.1"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "deterministic", "deploy", "promotion-gate", "prod", "OMN-13211"]
pack: "release"
display_name: "Prod Promotion Gate Compute"
node_role: "compute"
