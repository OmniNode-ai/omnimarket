name: node_verification_sweep_orchestrator
version: "1.0.0"
description: "Post-orchestration verification sweep — endpoints, database tables, DoD evidence"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_verification_sweep_orchestrator: "omnimarket.nodes.node_verification_sweep_orchestrator"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_write
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "verification", "post-orchestration", "dashboard", "database", "dod"]
pack: "sweep"
display_name: "Verification Sweep Orchestrator"
node_role: "orchestrator"
