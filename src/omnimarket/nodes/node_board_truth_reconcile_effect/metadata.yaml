name: node_board_truth_reconcile_effect
version: "1.0.0"
description: "Dry-run board reconciliation — reads the ledger, derives board truth, renders the diff table"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_board_truth_reconcile_effect: "omnimarket.nodes.node_board_truth_reconcile_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "board-truth", "projection", "ticket-triage"]
pack: "planning"
display_name: "Board Truth Reconcile"
node_role: "triage"
