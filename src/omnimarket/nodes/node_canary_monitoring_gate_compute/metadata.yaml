name: node_canary_monitoring_gate_compute
version: "0.1.0"
description: "Pure computation: classifies observed auth/TLS/broker/lag/RDS monitoring signal readings against per-signal threshold specs, reporting PASS/WARN/ABORT/BLOCKED_PENDING_A6. Scaffold only — numeric thresholds pending A6 contractor deliverable. Evidence-only, zero I/O."
pack: pipeline
node_role: compute
display_name: "Canary Monitoring Gate Compute"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_canary_monitoring_gate_compute: "omnimarket.nodes.node_canary_monitoring_gate_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "monitoring", "canary", "threshold", "managed-staging", "abort-gate"]
