name: node_pr_watch_orchestrator
version: "1.0.0"
description: "Orchestrates PR watch with polling, event emission, stateful tracking (STUB)"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_pr_watch_orchestrator: "omnimarket.nodes.node_pr_watch_orchestrator"
capabilities:
  standalone: false
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "pr", "watch", "polling"]
pack: "build"
display_name: "PR Watch Orchestrator"
node_role: "orchestrator"
