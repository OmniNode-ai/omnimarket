name: node_contractor_integration_note_effect
version: "1.0.0"
description: "Posts one idempotent Linear integration note per merge whose cited ticket is assigned to a configured external contractor"
pack: contractor_handoff
node_role: effect
display_name: "Contractor Integration Note"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_contractor_integration_note_effect: "omnimarket.nodes.node_contractor_integration_note_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: true
  requires_secrets: true
  requires_docker: false
  side_effect_class: external_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "integration-note", "contractor"]
