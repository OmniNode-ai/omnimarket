name: node_adr_extraction_grader_llm_effect
version: "1.0.0"
description: "Heuristic grader that uses Claude Opus to score ADR extraction quality across recall, precision, fidelity, and format_compliance dimensions."
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_adr_extraction_grader_llm_effect: "omnimarket.nodes.node_adr_extraction_grader_llm_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: external_io
dependencies:
  - "omnibase_core>=0.39.0"
  - "httpx>=0.27.0"
  - "anthropic>=0.25.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "adr", "grading", "llm", "heuristic", "canary"]
pack: "workflow"
display_name: "ADR Extraction Grader LLM Effect"
node_role: "effect"
