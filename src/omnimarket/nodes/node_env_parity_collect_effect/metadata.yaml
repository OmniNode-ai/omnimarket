name: node_env_parity_collect_effect
version: "1.0.0"
description: "Live read-only runtime-lane env collection + parity evaluation"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_env_parity_collect_effect: "omnimarket.nodes.node_env_parity_collect_effect"
capabilities:
  standalone: false
  full_runtime: false
  requires_network: true
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.40.0"
  - "pyyaml>=6.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "env", "parity", "lanes", "collection", "runtime"]
pack: "sweep"
display_name: "Env Parity Collect Effect"
node_role: "effect"
