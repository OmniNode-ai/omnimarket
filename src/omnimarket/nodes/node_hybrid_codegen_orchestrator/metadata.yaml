name: node_hybrid_codegen_orchestrator
version: "1.0.0"
description: "Codegen factory engine — an ORCHESTRATOR that composes spec -> generate -> validate -> type-check -> serialize -> write over the bus"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_hybrid_codegen_orchestrator: "omnimarket.nodes.node_hybrid_codegen_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  side_effect_class: orchestration
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "codegen", "factory"]
pack: "core"
display_name: "Hybrid Codegen Orchestrator"
node_role: "orchestrator"
