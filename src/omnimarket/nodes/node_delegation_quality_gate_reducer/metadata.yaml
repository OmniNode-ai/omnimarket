name: node_delegation_quality_gate_reducer
version: "1.0.0"
description: "Delegation quality gate reducer — validates LLM inference output against schema and compliance rules"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_delegation_quality_gate_reducer: "omnimarket.nodes.node_delegation_quality_gate_reducer"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
  - "omnibase-infra>=0.34.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["delegation", "quality-gate", "reducer", "validation"]
pack: "delegation"
display_name: "Delegation Quality Gate Reducer"
node_role: "reducer"
