name: node_llm_delegation_call_effect
version: "0.1.0"
description: "LLM delegation call effect — executes LLM API calls via httpx with health probe caching and cost telemetry"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_llm_delegation_call_effect: "omnimarket.nodes.node_llm_delegation_call_effect"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
  - "httpx>=0.27.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["llm", "delegation", "effect", "httpx", "cost-telemetry"]
pack: "delegation"
display_name: "LLM Delegation Call Effect"
node_role: "effect"
