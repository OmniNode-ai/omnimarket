registry_schema_version: "1.0.0"
endpoints:
  # OMN-16492: id kept as the stable registry key; model_id/provider/context
  # reflect the live endpoint. OMN-16999 re-probe 2026-09-05: GET /v1/models ->
  # http=200, {"id": "Qwen3.6-35B-A3B", "owned_by": "vllm", "max_model_len":
  # 131072}. This supersedes the OMN-16419 readback of 2026-08-23 ({"id":
  # "qwen3.8", "owned_by": "sglang", "max_model_len": 122880}) — the endpoint was
  # redeployed SGLang -> vLLM, hence `provider: vllm` below. Carries the
  # reasoning/math capabilities of the retired .201:8001 entry (GPU1 llama.cpp
  # decommissioned, connection refused — OMN-16442): this :8000 endpoint is the
  # local one now serving those task classes.
  - id: "qwen3.6-35b-a3b"
    base_url: "http://192.168.86.201:8000/v1"  # onex-allow-internal-ip OMN-11811 reason="lab LAN endpoint declared in operator registry contract"
    model_id: "Qwen3.6-35B-A3B"  # onex-allow-model-id OMN-11959 reason="operator registry must pin exact served model ID"
    endpoint_ref: "LLM_LOCAL_PRIMARY_URL"
    provider: "vllm"
    capabilities: [code_generation, structured_output, refactoring, analysis, reasoning, math, general]
    context_window: 131072
    context_window_source: "probe"
    cost_basis: "local"
    health_check_path: "/health"
    declared_by: "operator"
  - id: "text-embedding-gte"
    base_url: "http://192.168.86.201:8002/v1"  # onex-allow-internal-ip OMN-11811 reason="lab LAN endpoint declared in operator registry contract"
    model_id: "text-embedding-gte"  # onex-allow-model-id OMN-11959 reason="operator registry must pin exact vLLM-served model ID"
    endpoint_ref: "LLM_LOCAL_EMBEDDINGS_URL"
    provider: "vllm"
    capabilities: [embeddings]
    context_window: 8192
    context_window_source: "probe"
    cost_basis: "local"
    health_check_path: "/health"
    declared_by: "operator"
  - id: "deepseek-v4-flash"
    base_url: "http://192.168.86.200:8101/v1"  # onex-allow-internal-ip OMN-11811 reason="lab LAN endpoint declared in operator registry contract"
    model_id: "deepseek-v4-flash"
    endpoint_ref: "LLM_LOCAL_FAST_URL"
    provider: "ds4"
    capabilities: [reasoning, analysis, math, planning, general]
    context_window: 131072
    context_window_source: "probe"
    cost_basis: "local"
    health_check_path: "/health"
    declared_by: "operator"
  - id: "deepseek-v4-pro"
    base_url: "http://192.168.86.200:8101/v1"  # onex-allow-internal-ip OMN-11811 reason="lab LAN endpoint declared in operator registry contract"
    model_id: "deepseek-v4-pro"
    endpoint_ref: "LLM_REMOTE_FRONTIER_URL"
    provider: "ds4"
    capabilities: [reasoning, analysis, math, planning, general]
    context_window: 131072
    context_window_source: "probe"
    cost_basis: "local"
    health_check_path: "/health"
    declared_by: "operator"
