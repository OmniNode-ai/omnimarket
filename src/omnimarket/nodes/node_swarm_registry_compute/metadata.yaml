name: node_swarm_registry_compute
version: "1.0.0"
description: "Pure deterministic swarm endpoint selector — no LLM calls, no I/O, no side effects"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_swarm_registry_compute: "omnimarket.nodes.node_swarm_registry_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
  - "omnibase-compat>=0.3.1"
  - "pyyaml>=6.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "swarm", "endpoint-selection", "registry", "deterministic"]
pack: "core"
display_name: "Swarm Registry"
node_role: "compute"
