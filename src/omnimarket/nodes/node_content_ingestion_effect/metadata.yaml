name: node_content_ingestion_effect
version: "0.1.0"
description: "Content-type-aware extraction pipeline — consumes content-discovered events, routes by type, emits content-extracted events"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_content_ingestion_effect: "omnimarket.nodes.node_content_ingestion_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["ingestion", "effect", "content-type", "extraction", "python", "markdown", "yaml"]
pack: "market"
display_name: "Content Ingestion Effect"
node_role: "effect"
