name: node_pr_merged_projection
version: "1.0.0"
description: "Projects GitHub PR-merged events into a queryable monotonic-cursor projection for per-machine worktree reaper polling"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_pr_merged_projection: "omnimarket.nodes.node_pr_merged_projection"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: read_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["pr-merged", "projection", "reducer", "github", "worktree-reaper"]
pack: "platform"
display_name: "PR Merged Projection"
node_role: "reducer"
