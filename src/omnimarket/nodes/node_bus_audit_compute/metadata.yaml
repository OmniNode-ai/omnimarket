name: node_bus_audit_compute
version: "1.0.0"
description: "Audits event bus topic registry and node contract wiring completeness"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_bus_audit_compute: "omnimarket.nodes.node_bus_audit_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "bus", "audit", "event-bus", "wiring"]
pack: "sweep"
display_name: "Bus Audit Compute"
node_role: "compute"
