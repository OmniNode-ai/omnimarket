name: node_llm_delegation_projection
version: "1.0.0"
description: "Materializes daily LLM delegation aggregates from delegation-call-completed events"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_llm_delegation_projection: "omnimarket.nodes.node_llm_delegation_projection"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["llm-delegation", "projection", "reducer", "daily-aggregates", "cost-tracking"]
pack: "data"
display_name: "LLM Delegation Projection"
node_role: "reducer"
