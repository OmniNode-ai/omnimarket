name: node_projection_delegation_inference_response
version: "1.0.0"
description: "Materializes delegation inference response text snapshots from inference-response events (OMN-13088, NC-15)"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_projection_delegation_inference_response: "omnimarket.nodes.node_projection_delegation_inference_response"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["delegation", "projection", "reducer", "inference-response", "snapshot", "dashboard"]
pack: "data"
display_name: "Delegation Inference Response Projection"
node_role: "reducer"
