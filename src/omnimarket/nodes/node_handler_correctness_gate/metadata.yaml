name: node_handler_correctness_gate
version: "1.0.0"
description: "Pure compute node that scores pre-computed handler outputs against an eval set"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_handler_correctness_gate: "omnimarket.nodes.node_handler_correctness_gate"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: pure
dependencies:
  - "pydantic>=2.0"
authors: ["OmniNode Team"]
license: "MIT"
tags: ["compute", "eval", "correctness", "gate", "OMN-12130"]
pack: "eval"
display_name: "Handler Correctness Gate"
node_role: "compute"
