name: node_build_dispatch_effect
version: "1.0.0"
description: "Build dispatch effect — dispatches ticket-pipeline builds via delegation"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_build_dispatch_effect: "omnimarket.nodes.node_build_dispatch_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
  - "pyyaml>=6.0.2"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "build-loop", "delegation", "dispatch"]
