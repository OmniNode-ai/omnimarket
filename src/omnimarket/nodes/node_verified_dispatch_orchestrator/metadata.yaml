name: node_verified_dispatch_orchestrator
version: "1.0.0"
description: "Verified Dispatch Orchestrator — paired worker/verifier subagent dispatch with bounded escalation"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_verified_dispatch_orchestrator: "omnimarket.nodes.node_verified_dispatch_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
  - "pydantic>=2.0.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["dispatch", "verification", "worker", "verifier", "orchestrator", "OMN-11220"]
pack: verification
display_name: Verified Dispatch Orchestrator
node_role: orchestrator
