name: node_routing_policy_engine
version: "1.0.0"
description: "Computes retry policy decisions from failure class and attempt count"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_routing_policy_engine: "omnimarket.nodes.node_routing_policy_engine"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["routing", "retry", "policy", "compute"]
pack: "routing"
display_name: "Routing Policy Engine"
node_role: "compute"
