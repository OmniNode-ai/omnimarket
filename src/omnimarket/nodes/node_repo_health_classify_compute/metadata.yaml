name: node_repo_health_classify_compute
version: "1.0.0"
description: "Pure computation: classifies a CI/pre-commit failure into a failure-origin bucket with zero I/O"
pack: repo_health
node_role: compute
display_name: "Repo Health Classify"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_repo_health_classify_compute: "omnimarket.nodes.node_repo_health_classify_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "repo-health", "classify"]
