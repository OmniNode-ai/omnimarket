name: node_session_phase_reducer
version: "1.0.0"
description: "Session phase state reducer — pure delta(state, event) -> new_state materializing canonical phase state"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_session_phase_reducer: "omnimarket.nodes.node_session_phase_reducer"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: file_write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["reducer", "session", "phase"]
pack: "session"
display_name: "Session Phase Reducer"
node_role: "reducer"
