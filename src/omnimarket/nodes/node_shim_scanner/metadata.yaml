name: node_shim_scanner
version: "1.0.0"
description: "AST-based scanner for @shim decorator annotations — reports expired and expiring shims"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_shim_scanner: "omnimarket.nodes.node_shim_scanner"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "shim", "scanner", "ast", "tech-debt", "lifecycle"]
pack: "audit"
display_name: "Shim Scanner"
node_role: "compute"
