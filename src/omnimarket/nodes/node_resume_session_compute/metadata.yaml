name: node_resume_session_compute
version: "1.0.0"
description: "Load projected session state by task_id and agent_id (STUB)"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_resume_session_compute: "omnimarket.nodes.node_resume_session_compute"
capabilities:
  standalone: false
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "session", "resume", "state", "projection"]
pack: "session"
display_name: "Resume Session Compute"
node_role: "compute"
