name: node_projection_live_events
version: "1.0.0"
description: "Projects platform-wide bus events into a queryable live_events table for the dashboard live-event-stream widget"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_projection_live_events: "omnimarket.nodes.node_projection_live_events"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["live-events", "projection", "reducer", "observability", "dashboard"]
pack: "data"
display_name: "Live Events Projection"
node_role: "reducer"
