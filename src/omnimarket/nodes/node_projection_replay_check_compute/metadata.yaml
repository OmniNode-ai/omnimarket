name: node_projection_replay_check_compute
version: "1.0.0"
description: "Classifies projection events by replay/idempotence status (OMN-12884)"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_projection_replay_check_compute: "omnimarket.nodes.node_projection_replay_check_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "projection", "replay", "idempotence", "dedupe", "guardrail"]
pack: "projection"
display_name: "Projection Replay Check Compute"
node_role: "compute"
