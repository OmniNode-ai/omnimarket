name: node_staging_readiness_compute
version: "1.0.0"
description: "Evaluates a caller-supplied staging snapshot against a typed composition contract — pure COMPUTE"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_staging_readiness_compute: "omnimarket.nodes.node_staging_readiness_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "staging", "readiness", "preflight", "cloud"]
pack: "sweep"
display_name: "Staging Readiness Compute"
node_role: "compute"
