name: node_session_phase_dispatcher
version: "1.0.0"
description: "Session phase dispatcher — publishes phase-state and budget-warning events on transition"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_session_phase_dispatcher: "omnimarket.nodes.node_session_phase_dispatcher"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: event_publish
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "session-phase", "dispatcher"]
pack: "session"
display_name: "Session Phase Dispatcher"
node_role: "effect"
