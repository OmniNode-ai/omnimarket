name: node_closeout_effect
version: "1.0.0"
description: "Close-out phase effect — merge-sweep, quality gates, release readiness"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_closeout_effect: "omnimarket.nodes.node_closeout_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "build-loop", "closeout", "merge-sweep"]
