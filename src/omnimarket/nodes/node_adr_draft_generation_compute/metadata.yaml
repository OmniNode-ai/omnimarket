name: node_adr_draft_generation_compute
version: "1.0.0"
description: "Deterministic ADR markdown renderer from ModelDecisionExtraction — no LLM calls"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_adr_draft_generation_compute: "omnimarket.nodes.node_adr_draft_generation_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "adr", "canary", "deterministic", "markdown"]
pack: "canary"
display_name: "ADR Draft Generation"
node_role: "compute"
