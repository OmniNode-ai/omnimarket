name: node_repo_health_repair_effect
version: "1.0.0"
description: "Repo-health repair EFFECT node — emits a durable Linear repair task for REPO_BASELINE failures (OMN-13584)"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_repo_health_repair_effect: "omnimarket.nodes.node_repo_health_repair_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: true
  requires_docker: false
  side_effect_class: external_io
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "repo-health", "repair", "linear", "idempotent", "OMN-13584"]
pack: "workflow"
display_name: "Repo Health Repair Effect"
node_role: "effect"
