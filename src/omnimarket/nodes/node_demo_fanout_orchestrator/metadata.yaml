name: node_demo_fanout_orchestrator
version: "1.0.0"
description: "Fan-out LLM inference across multiple model configs and collect per-model results"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_demo_fanout_orchestrator: "omnimarket.nodes.node_demo_fanout_orchestrator"
capabilities:
  standalone: false
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["demo", "fanout", "orchestrator", "llm"]
pack: "demo"
display_name: "Demo Fan-out Orchestrator"
node_role: "orchestrator"
