name: node_dispatch_request_handler
version: "1.0.0"
description: "Routes dashboard dispatch requests to target nodes via command type routing"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_dispatch_request_handler: "omnimarket.nodes.node_dispatch_request_handler"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "dispatch", "dashboard", "routing"]
pack: "dispatch"
display_name: "Dispatch Request Handler"
node_role: "orchestrator"
