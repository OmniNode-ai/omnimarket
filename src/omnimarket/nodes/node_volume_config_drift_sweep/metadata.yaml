name: node_volume_config_drift_sweep
version: "1.0.0"
description: "Volume-vs-packaged config drift detection — compares deployed volume config against packaged source and classifies drift"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_volume_config_drift_sweep: "omnimarket.nodes.node_volume_config_drift_sweep"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["sweep", "config", "drift", "provenance", "delegation"]
pack: "infrastructure"
display_name: "Volume Config Drift Sweep"
node_role: "inventory"
