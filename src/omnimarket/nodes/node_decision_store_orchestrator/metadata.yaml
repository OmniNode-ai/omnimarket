name: node_decision_store_orchestrator
version: "1.0.0"
description: "Decision store orchestrator — conflict check, LLM review, persist, notify"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_decision_store_orchestrator: "omnimarket.nodes.node_decision_store_orchestrator"
capabilities:
  standalone: false
  full_runtime: false
  requires_network: true
dependencies:
  - "asyncpg>=0.31.0"
  - "omnibase_core>=0.40.0"
  - "omnibase-infra>=0.1.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "decision-store", "conflict-detection", "architecture", "governance"]
pack: "build"
display_name: "Decision Store Orchestrator"
node_role: "orchestrator"
