name: node_cross_cli_originator
version: "1.0.0"
description: "Shared CLI entry point invoked by opencode/codex plugins — publishes delegation envelope to event bus"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_cross_cli_originator: "omnimarket.nodes.node_cross_cli_originator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: write
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["effect", "cross-cli", "delegation", "originator", "kafka"]
pack: "delegation"
display_name: "Cross-CLI Originator"
node_role: "effect"
