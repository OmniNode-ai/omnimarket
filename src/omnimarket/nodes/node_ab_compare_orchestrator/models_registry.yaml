---
# models_registry.yaml — A/B compare model registry.
#
# Local models declare endpoint_env / model_id_env (env var keys) and
# requires_config_keys listing the vars that must be set for this entry to be
# active. All local-model resolution happens at runtime from environment
# variables. The cloud entry (glm-4.5) retains literal endpoint / model_id
# fallbacks alongside its env-var overrides for zero-config cloud access.
# protocol must currently be openai_compatible for orchestrator execution.
# requires_key names the API-key env var; models with missing keys are skipped.

schema_version: "1.0.0"
models:
  - id: qwen3-coder-30b
    display_name: "Qwen3-Coder-30B"
    endpoint_env: LLM_CODER_URL
    protocol: openai_compatible
    model_id_env: LLM_CODER_MODEL_NAME
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    location: local
    context_window: 112000
    requires_config_keys:
      - LLM_CODER_URL
      - LLM_CODER_MODEL_NAME

  - id: deepseek-r1-14b
    display_name: "DeepSeek-R1-14B"
    endpoint_env: LLM_CODER_FAST_URL
    protocol: openai_compatible
    model_id_env: LLM_CODER_FAST_MODEL_NAME
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    location: local
    context_window: 24000
    requires_config_keys:
      - LLM_CODER_FAST_URL
      - LLM_CODER_FAST_MODEL_NAME

  - id: deepseek-r1-32b
    display_name: "DeepSeek-R1-32B"
    endpoint_env: LLM_DEEPSEEK_R1_URL
    protocol: openai_compatible
    model_id_env: LLM_DEEPSEEK_R1_MODEL_NAME
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    location: local
    context_window: 8192
    requires_config_keys:
      - LLM_DEEPSEEK_R1_URL
      - LLM_DEEPSEEK_R1_MODEL_NAME

  - id: qwen3-next-80b
    display_name: "Qwen3-Next-80B"
    endpoint_env: LLM_QWEN3_NEXT_URL
    protocol: openai_compatible
    model_id_env: LLM_QWEN3_NEXT_MODEL_NAME
    cost_per_1k_input: 0.0
    cost_per_1k_output: 0.0
    location: local
    context_window: 8192
    requires_config_keys:
      - LLM_QWEN3_NEXT_URL
      - LLM_QWEN3_NEXT_MODEL_NAME

  - id: glm-4.5
    display_name: "z.ai GLM-4.5"
    endpoint: "https://api.z.ai/api/coding/paas/v4"
    endpoint_env: LLM_GLM_URL
    protocol: openai_compatible
    model_id: "glm-4.5"
    model_id_env: LLM_GLM_MODEL_NAME
    cost_per_1k_input: 0.0005
    cost_per_1k_output: 0.0005
    location: cloud
    requires_key: LLM_GLM_API_KEY
    context_window: 131072
