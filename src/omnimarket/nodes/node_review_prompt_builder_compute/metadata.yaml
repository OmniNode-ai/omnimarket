name: node_review_prompt_builder_compute
version: "1.0.0"
description: "Builds the adversarial-review (system, user) prompt pair — pure COMPUTE"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_review_prompt_builder_compute: "omnimarket.nodes.node_review_prompt_builder_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "review", "prompt", "adversarial"]
pack: "review"
display_name: "Review Prompt Builder"
node_role: "compute"
