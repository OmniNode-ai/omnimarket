name: node_pr_arm_gate_compute
version: "1.0.0"
description: "Fail-closed COMPUTE ARM/WITHHOLD decider — the merge-queue governor's sole arm-gate"
pack: pr_lifecycle
node_role: arm_gate
display_name: "PR Arm Gate"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_pr_arm_gate_compute: "omnimarket.nodes.node_pr_arm_gate_compute"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "pr-lifecycle", "arm-gate", "merge-queue-governor"]
