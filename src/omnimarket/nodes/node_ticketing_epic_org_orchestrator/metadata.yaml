name: node_ticketing_epic_org_orchestrator
version: "1.0.0"
description: "Ticketing epic org orchestrator — organizes tickets into epics with sub-ticket decomposition (STUB)"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_ticketing_epic_org_orchestrator: "omnimarket.nodes.node_ticketing_epic_org_orchestrator"
capabilities:
  standalone: false
  full_runtime: false
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: orchestrates
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["orchestrator", "ticketing", "epic", "linear", "OMN-12202"]
pack: "build"
display_name: "Ticketing Epic Org Orchestrator"
node_role: "orchestrator"
