name: node_occ_autoauthor_window
version: "1.0.0"
description: "Pure computation: counts the trailing consecutive-clean streak over the OCC auto-authoring observation trail and reports flip-readiness (N=10). Evidence-only, zero I/O."
pack: occ_companion
node_role: compute
display_name: "OCC Auto-authoring Window"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_occ_autoauthor_window: "omnimarket.nodes.node_occ_autoauthor_window"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "occ", "autoauthor", "report-only"]
