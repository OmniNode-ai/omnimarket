---
name: node_omnigate_receipt_verifier
version: "1.0.0"
description: "Verifies inline OmniGate PR receipts and returns fixed decision JSON"
omnibase_core_compat: ">=0.40.1,<1.0.0"
entry_points:
  onex.nodes:
    node_omnigate_receipt_verifier: "omnimarket.nodes.node_omnigate_receipt_verifier"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.40.1"
  - "omnibase-infra>=0.34.2"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["omnigate", "compute", "verification", "github-action"]
pack: "omnigate"
display_name: "OmniGate Receipt Verifier"
node_role: "compute"
