name: node_delegation_escalation_decision_compute
version: "1.0.0"
description: "Pure deterministic delegation escalation/tier decision — no LLM calls, no I/O"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_delegation_escalation_decision_compute: "omnimarket.nodes.node_delegation_escalation_decision_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
  - "omnibase-compat>=0.3.1"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "delegation", "escalation", "routing", "deterministic"]
pack: "core"
display_name: "Delegation Escalation Decision"
node_role: "compute"
