name: node_chain_diff
version: "1.0.0"
description: "Pure golden chain comparison — detects missing events, unexpected events, order mismatches, topic mismatches"
omnibase_core_compat: ">=0.40.0,<1.0.0"
entry_points:
  onex.nodes:
    node_chain_diff: "omnimarket.nodes.node_chain_diff"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
dependencies:
  - "omnibase_core>=0.40.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "chain-diff", "golden-chain", "deterministic", "verification"]
pack: "pipeline"
display_name: "Chain Diff"
node_role: "compute"
