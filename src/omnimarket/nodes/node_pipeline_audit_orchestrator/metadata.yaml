name: node_pipeline_audit_orchestrator
version: "1.0.0"
description: "Multi-repo pipeline audit orchestrator with parallel dispatch and gap register"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_pipeline_audit_orchestrator: "omnimarket.nodes.node_pipeline_audit_orchestrator"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: true
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: effectful
dependencies:
  - "omnibase_core>=0.39.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["pipeline-audit", "integration", "orchestrator", "multi-repo"]
pack: "pipeline-audit"
display_name: "Pipeline Audit Orchestrator"
node_role: "orchestrator"
