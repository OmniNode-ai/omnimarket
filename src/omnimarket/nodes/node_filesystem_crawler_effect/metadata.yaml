name: node_filesystem_crawler_effect
version: "0.1.0"
description: "Crawls configured filesystem paths and emits document lifecycle events"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_filesystem_crawler_effect: "omnimarket.nodes.node_filesystem_crawler_effect"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_repo: true
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies:
  - "omnibase_core>=0.39.0"
  - "omninode-memory>=0.1.0"
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["memory", "filesystem", "crawler", "document", "effect", "content-type", "crawl"]
pack: "memory"
display_name: "Filesystem Crawler Effect"
node_role: "effect"
