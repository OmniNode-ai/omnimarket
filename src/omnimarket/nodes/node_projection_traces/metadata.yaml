name: node_projection_traces
version: "1.0.0"
description: "Aggregate correlation-grouped log entries into trace snapshots for the dashboard trace-explorer."
omnibase_core_compat: ">=0.39.0,<1.0.0"
capabilities:
  standalone: true
  full_runtime: true
  requires_network: false
  requires_secrets: false
  side_effect_class: read_only
tags:
  - projection
  - traces
  - observability
  - snapshot
pack: "data"
display_name: "Projection: Traces"
node_role: "reducer"
