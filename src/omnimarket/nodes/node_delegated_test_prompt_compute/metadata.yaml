name: node_delegated_test_prompt_compute
version: "0.1.0"
description: "Pure computation: builds the delegated test loop's WRITE and REPAIR prompts and response contract; refuses a bundle carrying a hidden test's name. Zero I/O."
pack: pipeline
node_role: compute
display_name: "Delegated Test Prompt Compute"
omnibase_core_compat: ">=0.39.0,<1.0.0"
entry_points:
  onex.nodes:
    node_delegated_test_prompt_compute: "omnimarket.nodes.node_delegated_test_prompt_compute"
capabilities:
  standalone: true
  full_runtime: false
  requires_network: false
  requires_repo: false
  requires_secrets: false
  requires_docker: false
  side_effect_class: read_only
dependencies: []
authors: ["OmniNode Platform Team"]
license: "MIT"
tags: ["compute", "delegated-test-loop"]
