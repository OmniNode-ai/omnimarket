# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
#
# Model Registry — Source of truth for LLM model capabilities and pricing.
# Version: 1.0.0
# Ticket: OMN-11779 (epic OMN-11771)
#
# This file is loaded at startup by ModelRegistryLoader.
# Its SHA-256 hash (model_registry_hash) is included in every delegation event.
#
# IMPORTANT: Pricing data is point-in-time. Always check pricing_observed_at
# before using cost estimates. Refresh before any pricing claim is used in routing.
#
# Hardcoding volatile model facts (pricing, context windows, model versions)
# in Python or YAML is prohibited — use this registry as the single source.

schema_version: "1.0.0"
model_registry_version: "1.3.0"
pricing_manifest_version: "2026-06-11-gemini-25-flash-lite"
observed_at: "2026-06-11T00:00:00Z"

models:
  qwen3-coder-30b:
    model_id: "qwen3-coder-30b"
    provider: "local"
    endpoint_env: "LLM_CODER_URL"
    model_name: "Qwen3.6-35B-A3B"
    context_window: 131072
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-30T00:00:00Z"
    source: "manual benchmark"
    notes: >
      Routing key retained for downstream contract stability; actual served model
      is Qwen3.6-35B-A3B (GPTQ-Int4) on .201:8000 via vLLM. Live-probed 2026-05-30.
      Zero marginal API cost. context_window=131072 (131K GPTQ config).

  deepseek-r1-14b:
    model_id: "deepseek-r1-14b"
    provider: "local"
    endpoint_env: "LLM_CODER_FAST_URL"
    model_name: "Qwen3.6-27B-MTP-IQ4_XS.gguf"
    context_window: 114688
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-30T00:00:00Z"
    source: "manual benchmark"
    notes: >
      Routing key retained for downstream contract stability; actual served model
      is Qwen3.6-27B-MTP-IQ4_XS.gguf on .201:8001 via llama.cpp. Live-probed 2026-05-30.
      env var LLM_CODER_FAST_URL is legacy-named but correct. context_window=114688.

  deepseek-r1-32b:
    model_id: "deepseek-r1-32b"
    provider: "local"
    endpoint_env: "LLM_DEEPSEEK_R1_URL"
    context_window: 8192
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-24T00:00:00Z"
    source: "manual benchmark"
    notes: >
      Stale slot — previously MLX-served on .200:8101. Superseded by ds-v4-flash
      (OMN-12492). Key retained for routing contract stability; downstream contracts
      should migrate to ds-v4-flash. LLM_DEEPSEEK_R1_URL env var still declared.

  ds-v4-flash:
    model_id: "ds-v4-flash"
    provider: "local"
    endpoint_env: "LLM_DS_V4_FLASH_URL"
    model_name: "deepseek-v4-flash"
    context_window: 65536
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-30T00:00:00Z"
    source: "manual benchmark"
    notes: >
      DeepSeek V4 Flash (284B MoE) served by antirez/ds4 on .200:8101 (Mac Studio).
      Live-probed 2026-05-30 as the active .200:8101 endpoint.
      Zero marginal API cost. context_window_budgeted=65536 (conservative).
      LLM_DS_V4_FLASH_URL env var — actual URL lives in bifrost_overrides.yaml overlay (local file, not committed).

  qwen3.6-35b:
    model_id: "qwen3.6-35b"
    provider: "local"
    endpoint_env: "LLM_QWEN3_NEXT_URL"
    model_name: "mlx-community/Qwen3.6-35B-A3B-8bit"
    context_window: 8192
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-24T00:00:00Z"
    source: "manual benchmark"
    notes: >
      Canonical reasoning-moe-35b local slot. Key for new routing policies.
      Keep qwen3-next-80b as a compatibility alias until downstream contracts migrate.

  qwen3-next-80b:
    model_id: "qwen3-next-80b"
    provider: "local"
    endpoint_env: "LLM_QWEN3_NEXT_URL"
    model_name: "mlx-community/Qwen3.6-35B-A3B-8bit"
    context_window: 8192
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-24T00:00:00Z"
    source: "manual benchmark"
    notes: >
      Compatibility alias for qwen3.6-35b. Key retained for routing key stability.
      Actual served model is mlx-community/Qwen3.6-35B-A3B-8bit (replaced Qwen3-Next-80B-A3B 2026-04-18).

  gemini-2.5-flash-lite:
    model_id: "gemini-2.5-flash-lite"
    provider: "google"
    endpoint_env: "GEMINI_API_URL"
    model_name: "gemini-2.5-flash-lite"
    served_model_names:
      # AI Studio (generativelanguage) serves the bare model id.
      ai_studio: "gemini-2.5-flash-lite"
      # Vertex AI requires the publisher-qualified resource path. Posting the
      # bare AI-Studio name to the Vertex environment 404s — the P2.7 failure
      # mode (OMN-12937: gemini-2.0-flash 404 on Vertex). Live Vertex resource:
      # publishers/google/models/gemini-2.5-flash-lite (Vertex model garden, 2026-06).
      vertex: "publishers/google/models/gemini-2.5-flash-lite"
    requires_api_key_env: "GEMINI_API_KEY"
    context_window: 1048576
    pricing_per_1m_input: "0.10"
    pricing_per_1m_output: "0.40"
    cost_basis: "cloud_api_cost"
    observed_at: "2026-06-11T00:00:00Z"
    source: "provider docs"
    notes: >
      Gemini 2.5 Flash Lite. 1M context window. GEMINI_API_KEY or GOOGLE_API_KEY required.
      Pricing: $0.10/1M input, $0.40/1M output (Google AI pricing 2026-06-11).
      endpoint_url empty in repo default; overlay fills https://generativelanguage.googleapis.com/v1beta/openai/.
      Routed via cloud-gemini-flash bifrost backend.
      OMN-12937: retargeted from gemini-2.0-flash, whose free tier was quota-exhausted
      (HTTP 429). The delegation inference effect sources the wire model id from this
      registry entry directly; gemini-2.5-flash-lite probes HTTP 200 on the deployed key.
      OMN-12972 (P2.7): served_model_names maps each serving environment to its exact
      Vertex/AI-Studio wire name. The key-path id stays stable; the call boundary picks
      the env-specific served name so neither environment receives a name it 404s on.
      model_name retains the AI-Studio (default) served name for single-name consumers.

  vertex-gemini-flash:
    model_id: "vertex-gemini-flash"
    provider: "vertex"
    endpoint_env: "BIFROST_VERTEX_GEMINI_ENDPOINT_URL"
    model_name: "gemini-2.5-flash-lite"
    requires_secret_ref: "llm.vertex.access_token"
    context_window: 1048576
    pricing_per_1m_input: "0.10"
    pricing_per_1m_output: "0.40"
    cost_basis: "cloud_api_cost"
    observed_at: "2026-06-11T00:00:00Z"
    source: "provider docs"
    notes: >
      Vertex-served Gemini 2.5 Flash Lite (OMN-12971). Burns the Vertex $500
      credit via Vertex AI. The outbound call carries Authorization: Bearer
      <token>, where the token is a short-lived OAuth access token minted from
      Application Default Credentials (ADC) and published to the secret store as
      llm.vertex.access_token. ADC delivery to the effects container is a mounted
      credential file (GOOGLE_APPLICATION_CREDENTIALS) exchanged for that token;
      the token VALUE is resolved fail-closed at the effect boundary and never
      committed. ADDITIVE next to gemini-2.5-flash-lite (AI Studio key path);
      both routes stay selectable (provider-agnostic). endpoint_url null in repo
      default; the overlay supplies the COMPLETE project+region Vertex
      OpenAI-compat URL via BIFROST_VERTEX_GEMINI_ENDPOINT_URL. ADC proven HTTP
      200 from .201. Routed via cloud-vertex-gemini bifrost backend.

  openrouter-qwen3-coder-480b:
    model_id: "openrouter-qwen3-coder-480b"
    provider: "openrouter"
    endpoint_env: "OPENROUTER_URL"
    model_name: "qwen/qwen3-coder:free"
    requires_api_key_env: "OPENROUTER_API_KEY"
    context_window: 262144
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-30T00:00:00Z"
    source: "openrouter catalog"
    notes: >
      OpenRouter free tier — Qwen3 Coder 480B. Cheap frontier direct for June 5
      model-experimentation campaign (OMN-12492). Zero API cost (free tier, provider
      bears inference cost). Rate-limited, no SLA. Requires OPENROUTER_API_KEY.
      Falls back to openrouter-glm-flash or local when key absent.

  llama-3.3-70b-free:
    model_id: "llama-3.3-70b-free"
    provider: "openrouter"
    endpoint_env: "OPENROUTER_URL"
    model_name: "meta-llama/llama-3.3-70b-instruct:free"
    requires_api_key_env: "OPENROUTER_API_KEY"
    context_window: 131072
    pricing_per_1m_input: "0.00"
    pricing_per_1m_output: "0.00"
    cost_basis: "zero_marginal_api_cost"
    observed_at: "2026-05-23T00:00:00Z"
    source: "openrouter catalog"
    notes: >
      OpenRouter free tier — Llama 3.3 70B Instruct. Rate-limited, no SLA.
      Zero API cost but provider bears inference cost. CI default.

  claude-sonnet-4-6:
    model_id: "claude-sonnet-4-6"
    provider: "anthropic"
    endpoint_env: "ANTHROPIC_DELEGATION_URL"
    context_window: 200000
    pricing_per_1m_input: "3.00"
    pricing_per_1m_output: "15.00"
    cost_basis: "cloud_api_cost"
    observed_at: "2026-05-23T00:00:00Z"
    source: "provider docs"
    notes: >
      Claude Sonnet 4.6. Frontier fallback. Metered CLOUD_API_COST.
      Pricing sourced from Anthropic docs 2026-05-23.

  claude-opus-4-6:
    model_id: "claude-opus-4-6"
    provider: "anthropic"
    endpoint_env: "ANTHROPIC_DELEGATION_URL"
    context_window: 1000000
    pricing_per_1m_input: "15.00"
    pricing_per_1m_output: "75.00"
    cost_basis: "cloud_api_cost"
    observed_at: "2026-05-23T00:00:00Z"
    source: "provider docs"
    notes: >
      Claude Opus 4.6. Baseline for opus-equivalent cost calculations.
      Metered CLOUD_API_COST. Pricing sourced from Anthropic docs 2026-05-23.
