# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
#
# bifrost_delegation.yaml — Delegation routing rules for Claude Code task classes.
#
# Maps task-class contracts to bifrost backend routing policies.
# Every gateway response MUST include the selected rule_id and config_version
# to make provenance auditable (OMN-10637 requirement R1).
#
# OMN-12815: every endpoint_url is the COMPLETE final URL incl. the full chat
# path (e.g. .../v1/chat/completions). It is posted VERBATIM at every call site
# — no in-code construction, append, or path-resolver exists anymore. Public
# cloud endpoints carry the complete URL here; site-specific LOCAL endpoints are
# null in the repo default and MUST be supplied COMPLETE (incl. /v1/chat/completions)
# by the overlay file written by the installer, usually
# ~/.omninode/delegation/bifrost_overrides.yaml. A bare base (no chat path) is a
# misconfiguration — resolution fails closed rather than constructing a path.
#
# See routing_tiers.yaml for tier escalation ladder.
#
# Tickets: OMN-10637, OMN-10638, OMN-10657, OMN-12815
config_version: "2.0.0"
schema_version: "bifrost_delegation.v1"
# Backend definitions — endpoint_url values are merged from the overlay at load time.
# model_name is null here; resolved at deploy time via bifrost_overrides.yaml overlay.
# backend_ids are capability-named (not model-named) so the public config never
# embeds site-specific model identifiers.
backends:
  # Local backends (vLLM on .201) — capability-named, model_name resolved via overlay
  - backend_id: local-coder
    endpoint_url: null
    model_name: null
    tier: local
    timeout_ms: 60000
    capabilities:
      - code_generation
      - refactoring
      - documentation
  - backend_id: local-reasoner
    endpoint_url: null
    model_name: null
    tier: local
    timeout_ms: 60000
    capabilities:
      - deep_reasoning
      - research
      - classification
  - backend_id: local-heavy-reasoning
    endpoint_url: null
    model_name: null
    tier: local
    timeout_ms: 60000
    capabilities:
      - deep_reasoning
      - reasoning
      - research
      - classification
      - documentation
  - backend_id: local-embedding
    endpoint_url: null
    model_name: null
    tier: local
    timeout_ms: 30000
    capabilities:
      - embedding
  # Cloud fallback backends
  - backend_id: cloud-sonnet
    endpoint_url: ""
    # Claude Sonnet via OAuth — no API key required (Claude Code uses OAuth)
    model_name: claude-sonnet-4-6
    tier: frontier_api
    timeout_ms: 60000
    capabilities:
      - code_generation
      - reasoning
      - documentation
      - research
  - backend_id: cloud-haiku
    endpoint_url: ""
    # Claude Haiku — cheapest frontier model for documentation/simple tasks
    model_name: claude-haiku-4-5-20251001
    tier: frontier_api
    timeout_ms: 30000
    capabilities:
      - documentation
      - summarization
      - simple_tasks
  - backend_id: cloud-glm
    endpoint_url: ""
    model_name: glm-z-ai
    tier: cheap_cloud
    timeout_ms: 60000
    capabilities:
      - code_generation
      - reasoning
      - research
  - backend_id: cloud-gemini-flash
    # OMN-12815: COMPLETE endpoint URL incl. the full chat path, posted VERBATIM.
    # No in-code construction/append; the routing authority returns this unchanged.
    endpoint_url: "https://generativelanguage.googleapis.com/v1beta/openai/chat/completions"
    model_name: "gemini-2.0-flash"
    api_key_env: GEMINI_API_KEY
    tier: cheap_cloud
    timeout_ms: 60000
    capabilities:
      - summarization
      - simple_tasks
      - document
    extra_headers:
      HTTP-Referer: "https://omninode.ai"
      X-Title: "OmniNode ONEX"
  # Local DS-V4-Flash backend — DeepSeek V4 Flash (284B MoE) on .200:8101 (OMN-12492)
  - backend_id: local-ds-v4-flash
    endpoint_url: null
    # endpoint_url null in repo default; overlay sets .200:8101  # onex-allow-internal-ip OMN-12492 reason="repo-default comment documenting local overlay target for DS-V4-Flash"
    model_name: null
    tier: local
    timeout_ms: 60000
    capabilities:
      - deep_reasoning
      - reasoning
      - research
      - code_generation
  # OpenRouter backends (OMN-7980) — uses OPENROUTER_API_KEY from env at dispatch time.
  # api_key_env declared so the bifrost adapter picks up the secret without hardcoding it.
  - backend_id: openrouter-glm-flash
    # OMN-12815: COMPLETE endpoint URL incl. the full chat path, posted VERBATIM.
    endpoint_url: "https://openrouter.ai/api/v1/chat/completions"
    model_name: "thudm/glm-4-9b-chat:free"
    api_key_env: OPENROUTER_API_KEY
    tier: cheap_cloud
    timeout_ms: 60000
    capabilities:
      - code_generation
      - reasoning
      - research
    extra_headers:
      HTTP-Referer: "https://omninode.ai"
      X-Title: "OmniNode ONEX Build Loop"
  # OpenRouter cheap-frontier-direct — Qwen3 Coder 480B (free tier, OMN-12492)
  - backend_id: openrouter-qwen3-coder-480b
    # OMN-12815: COMPLETE endpoint URL incl. the full chat path, posted VERBATIM.
    endpoint_url: "https://openrouter.ai/api/v1/chat/completions"
    model_name: "qwen/qwen3-coder:free"
    api_key_env: OPENROUTER_API_KEY
    tier: cheap_frontier
    timeout_ms: 120000
    capabilities:
      - code_generation
      - reasoning
      - research
      - refactoring
    extra_headers:
      HTTP-Referer: "https://omninode.ai"
      X-Title: "OmniNode ONEX Experiment"
  # CLI agent backends — dispatched via subprocess, not HTTP.
  - backend_id: cli-claude
    endpoint_url: ""
    model_name: claude-cli
    tier: cli_agents
    timeout_ms: 300000
    capabilities:
      - agent_delegation
      - complex_reasoning
  - backend_id: cli-opencode
    endpoint_url: ""
    model_name: opencode-cli
    tier: cli_agents
    timeout_ms: 300000
    capabilities:
      - agent_delegation
      - code_generation
# Routing rules — evaluated in ascending priority order.
# First matching rule wins. Each rule maps a task_class to an ordered
# list of backends to try.
#
# Required fields per rule (OMN-10637):
#   rule_id: stable UUID for audit provenance
#   task_class_contract_version: version of the contract this rule targets
#   backend_policy_version: version of the backend policy applied
#   fallback_policy: what to do when all backends in backend_ids fail
#   shadow_policy_id: UUID of the shadow policy used for A/B evaluation
routing_rules:
  # ── code_generation ──────────────────────────────────────────────────────
  # Route code generation to local coder first (zero API cost),
  # fall back to cloud Sonnet for reliability.
  - rule_id: "d4e5f6a7-0001-4000-8000-000000000001"
    priority: 10
    task_class: code_generation
    task_class_contract_version: "1.0.0"
    backend_policy_version: "2.0.0"
    match_operation_types:
      - chat_completion
      - completion
    match_capabilities:
      - code_generation
    latency_sla_ms: 30000
    backend_ids:
      - local-coder
      - cloud-sonnet
    fallback_policy:
      action: escalate_to_next_tier
      max_retries: 2
      on_exhaust: return_error
    shadow_policy_id: "e5f6a7b8-0001-4000-8000-000000000001"
  # ── test ────────────────────────────────────────────────────────────────
  # Unit-test generation is code generation with stricter quality gates.
  # Route locally first with a longer timeout because test artifacts regularly
  # approach the 30s boundary on quantized local models.
  - rule_id: "d4e5f6a7-0004-4000-8000-000000000004"
    priority: 15
    task_class: test
    task_class_contract_version: "1.0.0"
    backend_policy_version: "2.0.0"
    match_operation_types:
      - chat_completion
      - completion
    match_capabilities:
      - code_generation
    latency_sla_ms: 60000
    backend_ids:
      - local-coder
      - cloud-sonnet
    fallback_policy:
      action: escalate_to_next_tier
      max_retries: 1
      on_exhaust: return_error
    shadow_policy_id: "e5f6a7b8-0004-4000-8000-000000000004"
  # ── documentation ────────────────────────────────────────────────────────
  # Documentation tasks: local coder is capable; haiku is cheaper
  # cloud fallback for cost-sensitive runs.
  - rule_id: "d4e5f6a7-0002-4000-8000-000000000002"
    priority: 20
    task_class: documentation
    task_class_contract_version: "1.0.0"
    backend_policy_version: "2.0.0"
    match_operation_types:
      - chat_completion
      - completion
    match_capabilities:
      - documentation
    cost_ceiling_usd_per_1k_tokens: 0.004
    backend_ids:
      - local-coder
      - cloud-haiku
    fallback_policy:
      action: escalate_to_next_tier
      max_retries: 1
      on_exhaust: return_error
    shadow_policy_id: "e5f6a7b8-0002-4000-8000-000000000002"
  # ── research ─────────────────────────────────────────────────────────────
  # Research tasks require deep reasoning; route to the heavy-reasoning local
  # slot first, then reasoner as secondary, then cloud Sonnet as fallback.
  - rule_id: "d4e5f6a7-0003-4000-8000-000000000003"
    priority: 30
    task_class: research
    task_class_contract_version: "1.0.0"
    backend_policy_version: "2.0.0"
    match_operation_types:
      - chat_completion
      - reasoning
    match_capabilities:
      - deep_reasoning
    backend_ids:
      - local-heavy-reasoning
      - local-reasoner
      - cloud-sonnet
    fallback_policy:
      action: escalate_to_next_tier
      max_retries: 2
      on_exhaust: return_error
    shadow_policy_id: "e5f6a7b8-0003-4000-8000-000000000003"
# Default fallback when no routing rule matches.
# Attempts backends in order.
default_backends:
  - local-coder
  - cloud-sonnet
# Gateway-level circuit breaker settings (apply to all backends).
circuit_breaker:
  failure_threshold: 5
  window_seconds: 30
# Gateway-level failover settings.
failover:
  max_attempts: 3
  backoff_base_ms: 500
# Shadow mode for delegation A/B testing (OMN-10638).
# Disabled by default — prevents accidental learned-policy activation during demo.
# To enable: set enabled: true and provide a policy_version identifier.
# Shadow decisions are emitted as comparison events only and NEVER affect
# live routing output. Dashboard/eval must label them SHADOW (shadow_label field).
shadow_mode:
  enabled: false
  policy_version: "unknown"
  log_sample_rate: 1.0
  comparison_logging_enabled: true
  max_shadow_latency_ms: 5.0
