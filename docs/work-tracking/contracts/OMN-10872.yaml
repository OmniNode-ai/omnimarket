---
schema_version: "1.0.0"
ticket_id: OMN-10872
title: "DI refactor: HandlerNavigationHistoryReducer accepts ProtocolNavigationHistoryWriter"
summary: "Adds ProtocolNavigationHistoryWriter structural protocol; HandlerNavigationHistoryReducer.__init__ now accepts ProtocolNavigationHistoryWriter | None. Default construction unchanged. Pure code seam refactor, no runtime deploy required."
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "DI injection unit tests pass"
    command: "uv run pytest tests/test_di_injection_navigation_history_reducer.py -v"
  - kind: ci
    description: "CI pipeline green on omnimarket PR #693"
    command: "gh pr checks 693 --repo OmniNode-ai/omnimarket"
  - kind: manual
    description: "Pure code seam refactor — no runtime deploy required"
    command: "printf '%s\n' 'deploy gate: pure DI seam refactor — no docker exec or rpk topic produce required'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "DI injection unit tests pass: protocol satisfaction, injection routing, shutdown propagation, default-construction fallback."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/test_di_injection_navigation_history_reducer.py -v --tb=short -q"
  - id: dod-deploy
    description: "Pure code seam refactor — no runtime restart or Kafka topic change required"
    source: manual
    checks:
      - check_type: command
        check_value: "printf '%s\n' 'deploy gate: pure DI seam refactor OMN-10872 — no docker exec or rpk topic produce required'"
