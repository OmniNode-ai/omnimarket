---
schema_version: "1.0.0"
ticket_id: "OMN-13727"
title: "test(omnimarket): golden-chain coverage audit — node_slack_publish_effect secret fail-closed"
summary: >
  T3 coverage audit for node_slack_publish_effect (OMN-13723). Three of the four required
  golden-chain cases were already covered in PR #1515: (a) happy-path publish via mocked
  transport, (c) channel fail-closed, (d) idempotency dedup. Case (b) — secret fail-closed
  when SLACK_BOT_TOKEN is absent from the secret store — was missing. This PR adds
  test_missing_token_raises_runtime_error_no_post to TestSlackPublishSecretFailClosed,
  mocking resolve_api_key_async to return None and asserting RuntimeError is raised with
  no HTTP POST attempted. No production runtime, contract, node, or event-bus topology change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-slack-publish-coverage"
    description: >
      omnimarket PR adding test_missing_token_raises_runtime_error_no_post to cover secret
      fail-closed (SLACK_BOT_TOKEN unset -> RuntimeError, no POST). 22/22 unit tests pass
      in tests/test_golden_chain_slack_publish_effect.py; ruff format+check clean; all four
      T3-required cases now covered.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13727/dod-omnimarket-slack-publish-coverage/command.yaml
          && grep -q '22/22 unit tests' drift/dod_receipts/OMN-13727/dod-omnimarket-slack-publish-coverage/command.yaml
  - id: "dod-occ-pr-self"
    description: >
      Self-binding OCC eligibility receipt for this onex_change_control PR carrying the
      OMN-13727 contract onto dev, satisfying the receipt-to-PR binding required by
      validator_occ_merge_eligibility.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13727/dod-occ-pr-self/command.yaml
          && grep -Eq '^pr_number: [0-9]+$' drift/dod_receipts/OMN-13727/dod-occ-pr-self/command.yaml
  - id: "dod-deploy-assessment"
    description: >
      Test-only PR; no new node, contract, runtime, event-bus topic, REST endpoint, or live
      deploy performed. This PR adds a single test method to the existing golden-chain test
      module. No deploy action required; no .201 mutation, no container restart, no runtime
      topology change. Normal CI merge pipeline applies.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13727/dod-deploy-assessment/command.yaml
          && grep -q 'No deploy action required' drift/dod_receipts/OMN-13727/dod-deploy-assessment/command.yaml
