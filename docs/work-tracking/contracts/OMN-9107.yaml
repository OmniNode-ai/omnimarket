---
schema_version: "1.0.0"
ticket_id: "OMN-9107"
title: "Add agent claim resolver node"
summary: >
  Add node_claim_resolver with typed request/response models and a CLI entry point
  for resolver-backed agent result verification used by omniclaude hook wiring.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - envelopes
evidence_requirements:
  - kind: "tests"
    description: "Claim resolver unit tests pass."
    command: "uv run pytest tests/unit/nodes/node_claim_resolver -q"
  - kind: "manual"
    description: "Claim resolver node files and console script registration pass ruff."
    command: "uv run ruff check src/omnimarket/nodes/node_claim_resolver tests/unit/nodes/node_claim_resolver pyproject.toml"
  - kind: "manual"
    description: "Post-merge deploy readiness is limited to runtime import and CLI invocation of node_claim_resolver; no live deploy or runtime restart was performed."
    command: "printf '%s\n' 'post-merge deploy readiness: runtime imports node_claim_resolver and CLI entry point without live runtime mutation'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-claim-resolver-tests"
    description: "Claim resolver unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_claim_resolver -q"
  - id: "dod-claim-resolver-lint"
    description: "Claim resolver node files and console script registration pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_claim_resolver tests/unit/nodes/node_claim_resolver pyproject.toml"
  - id: "dod-deploy-readiness"
    description: "Post-merge deploy readiness plan only; no live deploy or runtime restart was performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'post-merge deploy readiness: runtime imports node_claim_resolver and CLI entry point without live runtime mutation'"
