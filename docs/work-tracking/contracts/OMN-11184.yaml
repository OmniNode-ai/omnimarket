---
schema_version: "1.0.0"
ticket_id: "OMN-11184"
title: "Wire generation terminal events into delegation_events projection"
summary: >
  Subscribes node_projection_delegation to onex.evt.omnimarket.node-generation-completed.v1
  and projects benchmark payloads into a new generation_events table so node generation
  activity is visible on the delegation dashboard alongside FSM delegation events.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "db_schema"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-688"
    description: "Bind omnimarket PR #688 to OMN-11184 at observed head SHA."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 688 --repo OmniNode-ai/omnimarket --json headRefOid,state"
  - id: "dod-focused-tests"
    description: "Projection handler and golden-chain tests pass for generation event routing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_projection_handlers.py::TestDelegationHandler tests/test_golden_chain_projection_delegation.py -v"
  - id: "dod-deploy"
    description: "Migration 0008_generation_events.sql applied; projection runner restarted to pick up new topic subscription."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec omnimarket-projection grep -r 'node-generation-completed' /app/src/omnimarket/nodes/node_projection_delegation/contract.yaml"
