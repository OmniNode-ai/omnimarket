---
schema_version: "1.0.0"
ticket_id: OMN-13253
title: "Fix node_dod_verify dispatch so onex skill dod_verify runs"
summary: "Adds operation: verify and a top-level input_model to node_dod_verify's contract and renames the handler's first parameter to payload (with keyword-only evidence_results) so the RuntimeLocal event-driven path passes routing validation, seeds the required ticket_id into the initial command, and dispatches the command object positionally. onex skill dod_verify now completes with a structured ModelDodVerifyState instead of failing on a routing validation error."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "node_dod_verify unit tests pass"
    command: "uv run pytest tests/unit/nodes/node_dod_verify/ -q"
  - kind: ci
    description: "Runtime sweep validates all node entry points (operation_match entry now valid)"
    command: "uv run python scripts/ci/run_runtime_sweep.py"
  - kind: manual
    description: "Node contract + handler change. The onex skill / CLI (RuntimeLocal) dispatch path works immediately from the package; the deployed omninode-runtime daemon's dod-verify-start routing benefits after a redeploy (recommended post-merge, not required for the CLI/skill path). No Kafka topic or schema change."
    command: "printf '%s\n' 'deploy gate: node_dod_verify dispatch fix — CLI/skill path works from package; omninode-runtime redeploy recommended post-merge for dod-verify-start routing; no topic/schema change'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-tests
    description: "node_dod_verify unit tests pass (88 tests)"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/nodes/node_dod_verify/ -q"
  - id: dod-runtime-sweep
    description: "Runtime sweep reports all entry points OK (node_dod_verify contract validates)"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run python scripts/ci/run_runtime_sweep.py"
  - id: dod-deploy
    description: "Node contract+handler change; CLI/skill dispatch path works from package, omninode-runtime redeploy recommended post-merge for the daemon routing path. No topic/schema change."
    source: manual
    checks:
      - check_type: command
        check_value: "printf '%s\n' 'deploy gate: node_dod_verify dispatch fix; CLI path immediate; daemon redeploy recommended post-merge'"
