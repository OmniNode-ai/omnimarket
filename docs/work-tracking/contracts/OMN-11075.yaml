---
schema_version: "1.0.0"
ticket_id: "OMN-11075"
title: "Wait for scored delegation terminal payload in delegate-skill node"
summary: >
  Routes delegate-skill runtime dispatch directly to the delegation request
  topic and waits on the scored delegation-completed/failed terminal events.
  ModelDelegateSkillResponse exposes quality_score, total tokens,
  tokens_to_compliance, compliance_attempts, and cost_savings_usd estimation.
  Contract dependency topics reference the actual omnibase-infra delegation
  topics.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused delegate-skill orchestrator + delegate adapter + cross-repo contract dep tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_delegate_skill_orchestrator tests/unit/adapters/claude_code/test_delegate_adapter.py tests/ci/test_cross_repo_contract_deps.py -q"
  - id: "dod-deploy"
    description: >
      Runtime-path change rewires the delegate-skill orchestrator to wait on
      scored delegation terminal events. No live deploy or runtime restart was
      performed in this PR; post-merge validation should import the affected
      handler, response model, and port in the deployed runtime.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo deploy && uv run python -c \"from omnimarket.nodes.node_delegate_skill_orchestrator.handlers.handler_delegate_skill import HandlerDelegateSkill; from omnimarket.nodes.node_delegate_skill_orchestrator.models.model_delegate_skill_response import ModelDelegateSkillResponse; from omnimarket.nodes.node_delegate_skill_orchestrator.ports.port_runtime_delegation_dispatch import PortRuntimeDelegationDispatch; print(HandlerDelegateSkill.__name__, ModelDelegateSkillResponse.__name__, PortRuntimeDelegationDispatch.__name__)\""
