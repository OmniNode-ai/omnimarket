---
schema_version: "1.0.0"
ticket_id: OMN-10481
title: "Add ab-compare CLI wrapper"
summary: "Add a thin ab-compare CLI over the A/B compare orchestrator with table and JSON output while preserving node metadata, drift, and deploy-gate evidence."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Focused ab-compare CLI unit tests pass"
    command: "uv run pytest tests/unit/test_cli_ab_compare.py -q"
  - kind: "manual"
    description: "Node metadata dependency scope validates handler imports"
    command: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - kind: "manual"
    description: "Node drift gate validates changed node package registration"
    command: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - kind: "ci"
    description: "omnimarket PR #512 checks pass, including lint, deploy gate, and receipt gate"
    command: "gh pr checks 512 --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused ab-compare CLI unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/test_cli_ab_compare.py -q"
  - id: "dod-002"
    description: "Node metadata dependency scope validates handler imports"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - id: "dod-003"
    description: "Node drift gate validates changed node package registration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the ab-compare CLI and orchestrator handler in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.cli.cli_ab_compare import main; from omnimarket.nodes.node_ab_compare_orchestrator.handlers.handler_ab_compare_orchestrator import HandlerAbCompareOrchestrator; print('ab-compare deploy smoke ok')\""
