# OMN-11241 contract file for omnimarket deploy-gate (OMN-8912).
# The canonical ticket contract lives in onex_change_control.
# This file is the per-repo deploy-evidence carrier only.
schema_version: "1.0.0"
ticket_id: "OMN-11241"
title: "Harden contract registry stability path"
summary: "Enrich published event payloads on the contract registry handler register/reject paths with registration_request_id, event_type, contract_yaml, runtime_profile, and materialization_result for downstream projection and observability."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-handler-tests"
    description: "Contract registry handler tests pass with enriched payload"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/test_node_contract_registry/ -q"
  - id: "dod-deploy"
    description: "Handler import succeeds in deployed runtime — stability path hardening is handler-internal with no new service lifecycle"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec omninode-runtime python -c 'from omnimarket.nodes.node_contract_registry.handlers.handler_contract_registry import ContractRegistryHandler; print(\"handler ok\")'"
