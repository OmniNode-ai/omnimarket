---
schema_version: "1.0.0"
ticket_id: OMN-10486
title: "Classify receipt-only failures as OCC dependencies"
summary: "Teach PR lifecycle triage and orchestration to classify receipt-only failures as OCC dependency work, preserve ticket identity for evidence routing, and avoid dispatching code-fix work for those PRs."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Focused PR lifecycle OCC dependency tests pass"
    command: "PYTHONPATH=src uv run pytest tests/test_golden_chain_pr_lifecycle_triage_compute.py tests/test_lifecycle_reducer_kwarg_contract.py tests/test_golden_chain_pr_lifecycle_orchestrator.py tests/test_no_cross_node_reach_in.py -q"
  - kind: "manual"
    description: "Node metadata dependency scope validates changed PR lifecycle nodes"
    command: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - kind: "manual"
    description: "Node drift gate validates changed PR lifecycle node contracts"
    command: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - kind: "ci"
    description: "omnimarket PR #513 checks pass, including test, node drift, deploy gate, and receipt gate"
    command: "gh pr checks 513 --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused PR lifecycle OCC dependency tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=src uv run pytest tests/test_golden_chain_pr_lifecycle_triage_compute.py tests/test_lifecycle_reducer_kwarg_contract.py tests/test_golden_chain_pr_lifecycle_orchestrator.py tests/test_no_cross_node_reach_in.py -q"
  - id: "dod-002"
    description: "Node metadata dependency scope validates changed PR lifecycle nodes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - id: "dod-003"
    description: "Node drift gate validates changed PR lifecycle node contracts"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the changed PR lifecycle handlers and OCC dependency category"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_pr_lifecycle_orchestrator.handlers.handler_pr_lifecycle_orchestrator import HandlerPrLifecycleOrchestrator; from omnimarket.nodes.node_pr_lifecycle_state_reducer.handlers.handler_pr_lifecycle_state_reducer import HandlerPrLifecycleStateReducer; from omnimarket.nodes.node_pr_lifecycle_triage_compute.models.enum_pr_triage_category import EnumPrTriageCategory; assert EnumPrTriageCategory.OCC_DEPENDENCY.value == 'occ_dependency'; print('pr lifecycle OCC dependency deploy smoke ok')\""
