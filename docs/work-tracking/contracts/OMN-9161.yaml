---
schema_version: "1.0.0"
ticket_id: OMN-9161
title: "Propagate session/run id dispatch env through session orchestrator"
summary: "Carry session and run correlation through the session orchestrator dispatch environment while keeping event-bus topics contract-derived from node_session_orchestrator/contract.yaml."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Session orchestrator consumer and dispatch environment tests pass"
    command: "uv run pytest tests/test_session_orchestrator_consumer.py tests/unit/nodes/node_session_orchestrator/test_handler_dispatch_env.py -q"
  - kind: "manual"
    description: "Changed session orchestrator files pass ruff"
    command: "uv run ruff check src/omnimarket/nodes/node_session_orchestrator/handlers/handler_session_orchestrator.py tests/unit/nodes/node_session_orchestrator/test_handler_dispatch_env.py tests/test_session_orchestrator_consumer.py"
  - kind: "manual"
    description: "Post-merge deploy smoke plan verifies session orchestrator handler import and contract-derived terminal topics in the runtime container"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python - <<'PY'\nfrom omnimarket.nodes.node_session_orchestrator.handlers.handler_session_orchestrator import TOPIC_SESSION_ORCH_COMPLETED, TOPIC_SESSION_ORCH_FAILED, TOPIC_SESSION_ORCH_START, HandlerSessionOrchestrator\nprint('session orchestrator deploy smoke ok', HandlerSessionOrchestrator.__name__, TOPIC_SESSION_ORCH_START, TOPIC_SESSION_ORCH_COMPLETED, TOPIC_SESSION_ORCH_FAILED)\nPY"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-session-dispatch-env-tests"
    description: "Session orchestrator consumer and dispatch environment tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_session_orchestrator_consumer.py tests/unit/nodes/node_session_orchestrator/test_handler_dispatch_env.py -q"
  - id: "dod-ruff"
    description: "Changed session orchestrator files pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_session_orchestrator/handlers/handler_session_orchestrator.py tests/unit/nodes/node_session_orchestrator/test_handler_dispatch_env.py tests/test_session_orchestrator_consumer.py"
  - id: "dod-arch-topic-lint"
    description: "Changed handler no longer contains hardcoded event-bus topic literals"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "! rg '\"onex\\.(cmd|evt)\\.' src/omnimarket/nodes/node_session_orchestrator/handlers/handler_session_orchestrator.py"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke plan only; no live deploy or runtime restart performed in this PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python - <<'PY'\nfrom omnimarket.nodes.node_session_orchestrator.handlers.handler_session_orchestrator import TOPIC_SESSION_ORCH_COMPLETED, TOPIC_SESSION_ORCH_FAILED, TOPIC_SESSION_ORCH_START, HandlerSessionOrchestrator\nprint('session orchestrator deploy smoke ok', HandlerSessionOrchestrator.__name__, TOPIC_SESSION_ORCH_START, TOPIC_SESSION_ORCH_COMPLETED, TOPIC_SESSION_ORCH_FAILED)\nPY"
