# OMN-12795 contract file for omnimarket deploy-gate.
# The canonical ticket contract lives in onex_change_control.
# This file is the per-repo deploy-evidence carrier only.
---
schema_version: "1.0.0"
ticket_id: "OMN-12795"
title: "P2-4 — Selective guidance chunking: section parser (pure COMPUTE) + provenance fields"
summary: >
  Extend ModelContextPackArtifact with four optional provenance fields
  (source_file, heading_path, char_count, reason_selected) and add a pure
  GuidanceSectionParser (COMPUTE-eligible, zero filesystem I/O) that splits
  guidance files at ATX heading boundaries into typed ParsedSection objects,
  each convertible to a ModelContextPackArtifact via to_artifact(). The
  existing token-budget hard-reject (handler:157-165) is unchanged. Additive
  only — no existing callers break.
is_seam_ticket: false
interface_change: false
interfaces_touched:
  - "models"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-parser-tests-pass"
    description: >
      16 new unit tests covering GuidanceSectionParser (heading parsing, nesting,
      provenance fields, to_artifact(), hash stability) and ModelContextPackArtifact
      provenance field validation all pass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_context_pack_builder_compute/test_guidance_section_parser.py -v -q"
  - id: "dod-full-suite-no-regression"
    description: >
      Full test suite (6330 passed, 8 skipped, 0 failed) confirms no regressions
      from the ModelContextPackArtifact field additions or the new parsers package.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/ --tb=short -m 'not kafka' -q"
  - id: "dod-mypy-strict"
    description: >
      mypy --strict on the changed module (node_context_pack_builder_compute)
      reports 0 issues across 12 source files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnimarket/nodes/node_context_pack_builder_compute/ --strict"
  - id: "dod-pre-commit-clean"
    description: >
      All pre-commit hooks pass (ruff format, ruff check, topic lint, leaked-literal
      gate, orphan-graph gate, golden-chain coverage gate, end-of-file-fixer, etc.).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-no-filesystem-io-in-parser"
    description: >
      GuidanceSectionParser contains no open(), Path(), os.* or filesystem calls
      — verified by grep. File discovery/read is the EFFECT's responsibility.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -n 'open\\|Path\\|os\\.\\|pathlib' src/omnimarket/nodes/node_context_pack_builder_compute/parsers/parser_guidance_section.py && exit 1 || echo 'clean: no filesystem I/O in parser'"
