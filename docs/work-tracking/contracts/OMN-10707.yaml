---
schema_version: "1.0.0"
ticket_id: OMN-10707
title: "fix(OMN-10707): Remove bus bypass handler imports from merge sweep script"
summary: >
  scripts/run_orchestrated_merge_sweep_workflow.py had 7 direct imports from
  nodes.*.handlers.*, bypassing node public APIs. Populated __init__.py for 7 nodes
  with public exports and updated the script to import from node package roots.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "All omnimarket tests pass on the fix branch"
    command: "uv run pytest tests/ -x -q -m 'not kafka' --ignore=tests/test_datasource_postgres.py"
  - kind: ci
    description: "CI pipeline green on omnimarket PR #598"
    command: "gh pr checks 598 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-tests
    description: "3563 tests pass on fix branch; no handler-bypass imports remain"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/ -x -q -m 'not kafka' --ignore=tests/test_datasource_postgres.py"
  - id: dod-deploy
    description: >
      No live deploy required — this PR only updates __init__.py re-exports and
      import paths in a script. No runtime topology change, no Kafka topic addition,
      no container restart needed. Runtime picks up public API on next deploy cycle.
    source: manual
    checks:
      - check_type: command
        check_value: "printf '%s\n' 'deploy gate: import-path-only change; no docker exec or rpk topic produce required'"
