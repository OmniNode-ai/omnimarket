---
schema_version: "1.0.0"
ticket_id: OMN-10479
title: "Add ab-compare inference effect node"
summary: "Add the effect node that calls configured LLM endpoints for A/B comparison, measures token usage, and returns inference results with bounded error handling."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Focused ab-compare inference effect unit tests pass"
    command: "uv run pytest tests/nodes/node_ab_inference_effect/ -q"
  - kind: "manual"
    description: "Node metadata dependency scope validates inference effect imports"
    command: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - kind: "manual"
    description: "Node drift gate validates changed inference effect package registration"
    command: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - kind: "ci"
    description: "omnimarket PR #509 checks pass, including lint, deploy gate, and receipt gate"
    command: "gh pr checks 509 --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused ab-compare inference effect unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_ab_inference_effect/ -q"
  - id: "dod-002"
    description: "Node metadata dependency scope validates inference effect imports"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - id: "dod-003"
    description: "Node drift gate validates changed inference effect package registration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the inference effect handler in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_ab_inference_effect.handlers.handler_ab_inference_effect import HandlerAbInferenceEffect; print('ab-inference deploy smoke ok')\""
