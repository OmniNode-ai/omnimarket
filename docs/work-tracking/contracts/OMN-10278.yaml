---
schema_version: "1.0.0"
ticket_id: OMN-10278
title: "Mandatory DI params — resolver multi-param injection + handler migration"
summary: "Extend ServiceHandlerResolver Step 4 to inject any combination of known params (event_bus, container, ownership_query); remove = None defaults from injectable handler init params across omnibase_core, omnibase_infra, and omnimarket."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Unit tests green for fix-effect handler routing"
    command: "uv run pytest tests/nodes/node_pr_lifecycle_fix_effect/ -v"
  - kind: ci
    description: "CI pipeline green on omnimarket PR #596"
    command: "gh pr checks 596 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-pr-polish-delegation-wiring
    description: "pr_polish delegation wiring — code_failure and receipt_failure now route to dispatch_review_fix instead of gh run rerun"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/nodes/node_pr_lifecycle_fix_effect/ -v --tb=short"
  - id: dod-deploy
    description: "No live deploy required — this PR wires routing logic only; no runtime topology change, no Kafka topic addition, no container restart needed"
    source: manual
    checks:
      - check_type: command
        check_value: "printf '%s\n' 'deploy gate: routing-only change, no docker exec or rpk topic produce required; runtime picks up handler on next deploy cycle'"
