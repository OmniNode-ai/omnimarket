---
schema_version: "1.0.0"
ticket_id: "OMN-11046"
title: "Fix dependency health AST import scanner edge explosion"
summary: >
  Replaces prefix-based import matching with exact path-based module resolution
  so dependency health scans do not emit quadratic false-positive edges.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused dependency health scanner tests cover exact import resolution and bounded edge counts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/test_dep_health_ast_scanner.py -q"
  - id: "dod-deploy"
    description: >
      Runtime-path change is dependency health scanner logic only. No live
      deploy or runtime restart was performed in this PR; post-merge validation
      should import the affected scanner in the deployed runtime.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo deploy && uv run python -c \"from omnimarket.nodes.node_dependency_health_sweep.engine.ast_import_scanner import ASTImportScanner; print(ASTImportScanner.__name__)\""
