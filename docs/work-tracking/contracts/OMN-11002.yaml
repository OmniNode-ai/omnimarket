---
schema_version: "1.0.0"
ticket_id: "OMN-11002"
title: "Repair duplication sweep command drift"
summary: >
  Repairs the local duplication sweep D2/D3 command resolution so the sweep
  locates the current OCC Kafka boundary file and invokes the migration
  conflict checker instead of reporting command-surface drift.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-duplication-sweep-focused-tests"
    description: "Focused duplication sweep regression tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_duplication_sweep.py -q"
  - id: "dod-deploy"
    description: >
      Deploy evidence for a command-resolution-only runtime-path change. No
      live deploy, restart, or .201 mutation is required before merge; validate
      after merge by rerunning the D2/D3 duplication sweep command.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'post-merge deploy smoke: OMNI_HOME=$PWD uv run python -m omnimarket.nodes.node_duplication_sweep --omni-home $PWD --check D2,D3'"
