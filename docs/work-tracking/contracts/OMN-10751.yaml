---
schema_version: "1.0.0"
ticket_id: OMN-10751
title: "DI injection seam: HandlerOverseerVerifierConsumer"
summary: >
  HandlerOverseerVerifierConsumer.__init__ now accepts an optional verifier parameter
  (HandlerOverseerVerifier | None = None) with lifecycle-ok: optional-di-fallback annotation.
  Existing callers are unaffected; the default behavior is preserved when verifier is None.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "New DI injection test for HandlerOverseerVerifierConsumer passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_di_injection_overseer_verifier_consumer.py -v"
  - id: "dod-deploy"
    description: >
      Pure constructor-signature addition; no runtime behavior change. Deploy verification:
      import the changed handler module in the runtime container.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c 'from omnimarket.nodes.node_overseer_verifier.handlers.handler_overseer_verifier_consumer import HandlerOverseerVerifierConsumer; print(HandlerOverseerVerifierConsumer)'"
