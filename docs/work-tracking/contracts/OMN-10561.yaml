---
schema_version: "1.0.0"
ticket_id: OMN-10561
title: "Add generic contract-driven reducer node"
summary: "Adds node_contract_reducer as a pure COMPUTE node that interprets contract-declared transition tables without owning persistence or external I/O."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Reducer node type-checks under strict mypy"
    command: "uv run mypy src/omnimarket/nodes/node_contract_reducer/ --strict"
  - kind: "manual"
    description: "Reducer unit tests pass"
    command: "uv run pytest tests/unit/nodes/node_contract_reducer/ -q"
  - kind: "manual"
    description: "Reducer node sources pass ruff"
    command: "uv run ruff check src/omnimarket/nodes/node_contract_reducer tests/unit/nodes/node_contract_reducer"
  - kind: "manual"
    description: "Post-merge deploy proof plan only; live deploy/restart requires explicit operator approval"
    command: "deploy validation: run targeted reducer smoke after merge before any runtime promotion"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-mypy-strict"
    description: "Reducer node type-checks under strict mypy"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnimarket/nodes/node_contract_reducer/ --strict"
  - id: "dod-unit-tests"
    description: "Reducer unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_contract_reducer/ -q"
  - id: "dod-ruff"
    description: "Reducer node sources pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_contract_reducer tests/unit/nodes/node_contract_reducer"
  - id: "dod-deploy-proof-plan"
    description: "Post-merge deploy proof plan only; no live deploy or restart performed during PR validation"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy validation: run targeted reducer smoke after merge before any runtime promotion"
