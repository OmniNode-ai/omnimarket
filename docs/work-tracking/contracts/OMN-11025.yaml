---
schema_version: "1.0.0"
ticket_id: "OMN-11025"
title: "Migrate omnimarket delegation DTOs to omnibase_compat wire models"
summary: >
  Replaces duplicate omnimarket delegation DTO definitions with compatibility
  imports from canonical omnibase_compat.contracts.delegation.wire models while
  preserving existing omnimarket module paths for callers and contract refs.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-import-tests"
    description: "Focused delegation import, schema-ref, routing, projection, and budget tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_delegation_dto_omnibase_compat_imports.py tests/test_memory_dto_omnimemory_imports.py tests/unit/delegation tests/nodes/node_delegation_routing_reducer/test_contract_routing_policy.py tests/test_golden_chain_projection_delegation.py tests/unit/nodes/node_build_loop_orchestrator/test_dispatch_trace.py src/omnimarket/nodes/node_budget_policy_compute/tests/test_handler_budget_policy.py -q"
  - id: "dod-ruff"
    description: "Touched delegation compatibility files pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check pyproject.toml src/omnimarket/events/delegation.py src/omnimarket/adapters/llm/bifrost/model_bifrost_delegation_config.py src/omnimarket/nodes/node_budget_policy_compute/models/model_budget_limits.py src/omnimarket/nodes/node_delegation_orchestrator/models src/omnimarket/nodes/node_delegation_quality_gate_reducer/models src/omnimarket/nodes/node_delegation_routing_reducer/models src/omnimarket/nodes/node_delegation_routing_reducer/handlers/handler_delegation_routing.py src/omnimarket/nodes/node_projection_delegation/handlers/handler_projection_delegation.py src/omnimarket/nodes/node_build_loop_orchestrator/models/model_dispatch_trace.py tests/test_delegation_dto_omnibase_compat_imports.py"
  - id: "dod-d4-delegation-wire"
    description: >
      D4 overlay check with the OMN-11025 worktree and OMN-11024
      omnibase_compat source removes targeted delegation wire DTO collisions;
      unrelated residual D4 findings, including ModelRoutingDecision, remain
      outside this ticket scope.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "D4-delegation-wire-targeted status=PASS findings=[]"
  - id: "dod-deploy-gate"
    description: >
      Runtime-path change is model import compatibility only; no live deploy,
      restart, or .201 mutation required pre-merge. Post-merge validation should
      smoke import affected delegation node entry points on the deployed image.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'deploy gate: import compatibility only; no docker exec or rpk topic produce required pre-merge'"
