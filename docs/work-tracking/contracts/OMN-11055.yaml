---
schema_version: "1.0.0"
ticket_id: "OMN-11055"
title: "Enhance dry-run output with capability graph evidence"
summary: >
  Onboarding dry-run handler now returns execution_mode, requested_capabilities,
  and produced_capabilities_per_step in the rendered output. Golden chain + unit
  tests updated to assert all three new fields.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Onboarding golden chain + handler_onboarding tests pass with new dry-run fields."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_onboarding/ -q"
  - id: "dod-deploy"
    description: >
      Runtime-path change adds capability graph fields to the onboarding dry-run
      handler output. No live deploy or runtime restart was performed in this
      PR; post-merge validation should import the affected handler in the
      deployed runtime.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo deploy && uv run python -c \"from omnimarket.nodes.node_onboarding.handlers.handler_onboarding import HandlerOnboarding; print(HandlerOnboarding.__name__)\""
