---
schema_version: "1.0.0"
ticket_id: OMN-10693
title: "Build document ingestion node (read-only filesystem effect)"
summary: >
  EFFECT node node_adr_document_ingestion_effect added to omnimarket. Crawls markdown files
  recursively, computes sha256 content hashes, and extracts git metadata via async subprocess.
  Subscribes to adr-document-ingestion-requested.v1, publishes adr-document-ingestion-completed.v1.
  Read-only — no writes, no LLM, no network. 28 unit tests, all pre-commit hooks pass.
  Entry-point registered in pyproject.toml.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
evidence_requirements:
  - kind: "tests"
    description: "28 unit tests covering sha256, exclude patterns, git metadata, multi-root, and subprocess failure graceful fallback."
    command: "uv run pytest src/omnimarket/nodes/node_adr_document_ingestion_effect/tests/ -v -m 'not integration'"
  - kind: "ci"
    description: "All CI checks pass on omnimarket PR #562."
    command: "gh pr checks 562 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "Node registered in omnimarket entry-points for runtime deploy."
    command: "grep 'node_adr_document_ingestion_effect' pyproject.toml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "28 unit tests pass covering all handler logic, exclusion patterns, sha256, and git metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest src/omnimarket/nodes/node_adr_document_ingestion_effect/tests/ -v -m 'not integration' 2>&1 | grep '28 passed'"
  - id: "dod-pre-commit"
    description: "Pre-commit hooks pass for all new node files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-deploy-entry-point"
    description: "Node registered in pyproject.toml entry-points and ready to deploy via omnimarket runtime."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy validation: grep 'node_adr_document_ingestion_effect' pyproject.toml confirms entry-point registration for omnimarket runtime deploy"
