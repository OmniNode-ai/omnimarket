---
schema_version: "1.0.0"
ticket_id: OMN-10697
title: "Build Opus grading LLM effect node"
summary: >
  Adds node_adr_extraction_grader_llm_effect to omnimarket -- a heuristic grader that uses Claude Opus
  (or any configured model) to score ADR extraction output against a ground truth ADR across four dimensions:
  recall, precision, fidelity, and format_compliance. On grader failure returns success=False with typed
  error codes, never zero-scores. This is a new compute node (no runtime deploy required -- nodes are loaded
  dynamically from the omnimarket package).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests-pass"
    description: "14/14 unit tests pass for node_adr_extraction_grader_llm_effect with mocked Opus responses."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest src/omnimarket/nodes/node_adr_extraction_grader_llm_effect/tests/ -v --tb=short"
  - id: "dod-deploy"
    description: >
      No runtime deploy required -- node_adr_extraction_grader_llm_effect is a new omnimarket node loaded
      dynamically from the package. No container rebuild or daemon restart needed.
      deploy evidence: omnimarket PR #561 CI Summary passes.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 561 --repo OmniNode-ai/omnimarket --json statusCheckRollup --jq '.statusCheckRollup[] | select(.name == \"CI Summary\") | .conclusion' | grep -q SUCCESS # deploy evidence: no container rebuild required, node loads dynamically from package"
  - id: "dod-pr-561-head"
    description: "omnimarket PR #561 carries the node_adr_extraction_grader_llm_effect implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 561 --repo OmniNode-ai/omnimarket --json state,baseRefName --jq '.state + \":\" + .baseRefName' | grep -q 'OPEN:main'"
