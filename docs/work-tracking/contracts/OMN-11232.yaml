---
schema_version: "1.0.0"
ticket_id: OMN-11232
title: "feat(OMN-11232): create node_session_phase_orchestrator (ORCHESTRATOR)"
summary: >
  Creates node_session_phase_orchestrator — the tick-driven orchestrator for session phase lifecycle.
  Receives evaluation results (onex.evt.omnimarket.session-phase-evaluated.v1) and routes them:
  transition_required -> SESSION_PHASE_TRANSITION, halt_required -> SESSION_HALT_REQUIRED,
  no_action -> tick-completed only. Topics loaded from contract.yaml. 4 unit test classes covering
  all routing outcomes. Part of epic OMN-11224.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Implementation PR open against omnimarket main."
    command: "gh pr list --repo OmniNode-ai/omnimarket --head jonah/omn-11232-session-phase-orchestrator --json number,state,url"
  - kind: "manual"
    description: >
      ORCHESTRATOR node — subscribes to evaluated topic and publishes commands. No runtime service restart,
      no DB migration, no .201 mutation required. Node is passive until wired to evaluator.
    command: "grep -q '^node_type: orchestrator$' src/omnimarket/nodes/node_session_phase_orchestrator/contract.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-pass"
    description: "Implementation PR is open for OMN-11232 against omnimarket main."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr list --repo OmniNode-ai/omnimarket --head jonah/omn-11232-session-phase-orchestrator --json number,state | grep OPEN"
  - id: "dod-deploy-assessment"
    description: >
      ORCHESTRATOR node ships with passive subscription — no live deploy, runtime restart, .201 mutation,
      or docker exec required for this PR. Node activates when wired to the evaluator post-merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^node_type: orchestrator$' src/omnimarket/nodes/node_session_phase_orchestrator/contract.yaml"
