---
schema_version: "1.0.0"
ticket_id: OMN-10711
title: "Declare storage/vector config in contract.yaml"
summary: >
  Adds env_dependencies sections to four nodes whose handlers read storage/vector
  connection env vars without contract.yaml declaration: node_agent_coordinator_orchestrator
  (VALKEY_HOST), node_baseline_capture (OMNIBASE_INFRA_DB_URL, POSTGRES_HOST/PORT,
  VALKEY_HOST/PORT, QDRANT_HOST/PORT), node_navigation_history_reducer (QDRANT_HOST/PORT),
  node_projection_llm_cost (POSTGRES_HOST/PORT). Also adds missing handler block to
  node_navigation_history_reducer to fix node-drift-gate strict violation. Contract
  declarations only — no Python code changed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Full omnimarket unit test suite passes (2271 tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/ -m unit -q"
  - id: "dod-lint"
    description: "Ruff lint and format clean."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/ && uv run ruff format --check src/"
  - id: "dod-pre-commit"
    description: "All pre-commit hooks pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "OMNIBASE_INFRA_PATH=/Users/jonah/omni_home/omnibase_infra pre-commit run --all-files"
  - id: "dod-deploy"
    description: >
      Contract-only change (no Python modified). No live deploy required. Deploy-agent
      inactive per OMN-8841. Runtime validation deferred to next scheduled deploy cycle.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c 'import omnimarket; print(\"import ok\")'"
