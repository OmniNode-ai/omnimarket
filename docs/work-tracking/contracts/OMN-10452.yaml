---
schema_version: "1.0.0"
ticket_id: OMN-10452
title: "Fix intelligence orchestrator empty handler name in auto-wiring"
summary: "Add resolver-compatible handler names to node_intelligence_orchestrator function routing entries and test that the names are non-empty, profile-restricted, and import-resolve to the declared functions."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "manual"
    description: "Intelligence orchestrator runtime readiness tests pass"
    command: "uv run pytest tests/nodes/node_intelligence_orchestrator/test_contract_runtime_readiness.py -q"
  - kind: "manual"
    description: "Touched intelligence orchestrator runtime readiness test passes lint"
    command: "uv run ruff check tests/nodes/node_intelligence_orchestrator/test_contract_runtime_readiness.py"
  - kind: "manual"
    description: "Contract handler names resolve to non-empty auto-wiring identifiers"
    command: "uv run python - <<'PY'\nfrom pathlib import Path\nimport yaml\npath = Path('src/omnimarket/nodes/node_intelligence_orchestrator/contract.yaml')\nraw = yaml.safe_load(path.read_text(encoding='utf-8'))\nhandlers = raw['handler_routing']['handlers']\nprint([entry['handler']['name'] for entry in handlers])\nPY"
  - kind: "manual"
    description: "Post-merge deploy gate verifies stability image excludes the intelligence runtime-owned handler"
    command: "printf '%s\n' 'post-merge deploy smoke: RUNTIME_PROFILE=main runtime startup must skip node_intelligence_orchestrator'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-readiness-tests"
    description: "Intelligence orchestrator runtime readiness tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_intelligence_orchestrator/test_contract_runtime_readiness.py -q"
  - id: "dod-lint"
    description: "Touched intelligence orchestrator runtime readiness test passes lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check tests/nodes/node_intelligence_orchestrator/test_contract_runtime_readiness.py"
  - id: "dod-handler-names"
    description: "Contract handler names resolve to non-empty auto-wiring identifiers"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python - <<'PY'\nfrom pathlib import Path\nimport yaml\npath = Path('src/omnimarket/nodes/node_intelligence_orchestrator/contract.yaml')\nraw = yaml.safe_load(path.read_text(encoding='utf-8'))\nhandlers = raw['handler_routing']['handlers']\nprint([entry['handler']['name'] for entry in handlers])\nPY"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke plan for main-profile runtime startup; no live deploy or runtime restart was performed in this PR"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'post-merge deploy smoke: RUNTIME_PROFILE=main runtime startup must skip node_intelligence_orchestrator'"
