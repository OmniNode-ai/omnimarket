---
schema_version: "1.0.0"
ticket_id: OMN-10580
title: "Add CI grep gate for leaked literals with allowlist governance"
summary: >
  Harden the advisory check_leaked_literals.sh into a blocking gate wired as
  a pre-commit hook and a required GHA status check. Add inline annotation
  escape hatches and file-level exemptions for test fixtures. Annotate all
  existing src/ env-var fallbacks.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Blocking scan exits clean"
    command: "bash scripts/validation/check_leaked_literals.sh blocking all"
  - kind: "manual"
    description: "Unit tests pass"
    command: "uv run pytest tests/unit/scripts/test_leak_gate.py -q"
  - kind: "manual"
    description: "Pre-commit hook passes"
    command: "uv run pre-commit run leaked-literals-gate --all-files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gate-clean"
    description: "Blocking scan exits 0 with findings=0 across all files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "bash scripts/validation/check_leaked_literals.sh blocking all"
  - id: "dod-unit-tests"
    description: "Leak gate unit tests pass (12 tests)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/scripts/test_leak_gate.py -q"
  - id: "dod-pre-commit"
    description: "Pre-commit leaked-literals-gate hook passes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pre-commit run leaked-literals-gate --all-files"
  - id: "dod-deploy"
    description: >
      Source changes are annotation-only (# onex-allow-* comments added to
      existing env-var fallback lines); no runtime logic was altered. No deploy
      or runtime restart required. Gate satisfied by annotation-only diff.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy: annotation-only source changes — no runtime behaviour altered, no deploy or restart required'"
