---
schema_version: "1.0.0"
ticket_id: OMN-9877
title: "Harden session bootstrap runtime projection"
summary: "Adds runtime profile declarations to session bootstrap and related nodes, hardens handler behavior, extends golden-chain coverage for session bootstrap output."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Full omnimarket test suite passes (non-kafka)"
    command: "uv run pytest tests/ -m 'not kafka' -q"
  - kind: ci
    description: "CI pipeline green on omnimarket PR #645"
    command: "gh pr checks 645 --repo OmniNode-ai/omnimarket"
  - kind: manual
    description: "Runtime profile contracts valid — no deploy restart required (contract metadata only)"
    command: "printf '%s\n' 'deploy gate: session bootstrap runtime profile backfill — contract metadata change only; no docker exec or rpk topic produce required'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-tests
    description: "Full omnimarket test suite passes (non-kafka)"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/ -m 'not kafka' -q"
  - id: dod-deploy
    description: "Contract metadata change only — no runtime restart or Kafka topic change required"
    source: manual
    checks:
      - check_type: command
        check_value: "printf '%s\n' 'deploy gate: session bootstrap runtime profile backfill — contract metadata only; no docker exec or rpk topic produce required'"
