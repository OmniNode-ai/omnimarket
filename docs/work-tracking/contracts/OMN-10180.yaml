---
schema_version: "1.0.0"
ticket_id: OMN-10180
summary: "node_pr_polish live workflow runner — repo/worktree execution cutover"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass including node-drift-gate, typecheck, and test"
    command: "gh pr checks 437 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Golden-chain and CLI tests pass for node_pr_polish workflow runner"
    command: "uv run pytest tests/test_golden_chain_pr_polish.py tests/unit/nodes/node_pr_polish/ -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "CLI test proves live node resolves worktree and writes result.json"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pr_polish/test_main_cli.py -v"
  - id: "dod-002"
    description: "Golden-chain round-trip for node_pr_polish passes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_pr_polish.py -v"
  - id: "dod-003"
    description: "Post-merge deploy smoke: pr_polish node imports cleanly in deployed runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_pr_polish.workflow_runner import run_live_pr_polish; print('deploy smoke ok')\""
